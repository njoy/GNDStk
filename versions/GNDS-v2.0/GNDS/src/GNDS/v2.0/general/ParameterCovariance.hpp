
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef GNDS_V2_0_GENERAL_PARAMETERCOVARIANCE
#define GNDS_V2_0_GENERAL_PARAMETERCOVARIANCE

#include "GNDS/v2.0/general/RowData.hpp"
#include "GNDS/v2.0/general/ParameterCovarianceMatrix.hpp"

namespace GNDS {
namespace v2_0 {
namespace general {

// -----------------------------------------------------------------------------
// general::
// class ParameterCovariance
// -----------------------------------------------------------------------------

class ParameterCovariance :
   public Component<general::ParameterCovariance>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "general"; }
   static auto CLASS() { return "ParameterCovariance"; }
   static auto NODENAME() { return "parameterCovariance"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // metadata
         std::string{}
            / Meta<>("label") |

         // children
         --Child<general::RowData>
            ("rowData") |
         --Child<general::ParameterCovarianceMatrix>
            ("parameterCovarianceMatrix")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "label",
         "rowData",
         "parameterCovarianceMatrix"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "label",
         "row_data",
         "parameter_covariance_matrix"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // metadata
   Field<std::string>
      label{this};

   // children
   Field<general::RowData>
      rowData{this};
   Field<general::ParameterCovarianceMatrix>
      parameterCovarianceMatrix{this};

   // shortcuts
   #define NJOY_GNDSTK_SHORTCUT(to,name) decltype(to.name) &name = to.name
   NJOY_GNDSTK_SHORTCUT(parameterCovarianceMatrix(),array);
   NJOY_GNDSTK_SHORTCUT(parameterCovarianceMatrix().array(),lengths);
   NJOY_GNDSTK_SHORTCUT(parameterCovarianceMatrix().parameters(),parameterLink);
   NJOY_GNDSTK_SHORTCUT(parameterCovarianceMatrix(),parameters);
   NJOY_GNDSTK_SHORTCUT(rowData(),slices);
   NJOY_GNDSTK_SHORTCUT(parameterCovarianceMatrix().array(),starts);
   NJOY_GNDSTK_SHORTCUT(parameterCovarianceMatrix().array(),values);
   #undef NJOY_GNDSTK_SHORTCUT

   // ------------------------
   // Constructors
   // ------------------------

   #define NJOY_GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->label, \
      this->rowData, \
      this->parameterCovarianceMatrix \
   )

   // default
   ParameterCovariance() :
      NJOY_GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit ParameterCovariance(
      const wrapper<std::string>
         &label,
      const wrapper<general::RowData>
         &rowData = {},
      const wrapper<general::ParameterCovarianceMatrix>
         &parameterCovarianceMatrix = {}
   ) :
      NJOY_GNDSTK_COMPONENT(BlockData{}),
      label(this,label),
      rowData(this,rowData),
      parameterCovarianceMatrix(this,parameterCovarianceMatrix)
   {
      Component::finish();
   }

   // from node
   explicit ParameterCovariance(const Node &node) :
      NJOY_GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   ParameterCovariance(const ParameterCovariance &other) :
      NJOY_GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      label(this,other.label),
      rowData(this,other.rowData),
      parameterCovarianceMatrix(this,other.parameterCovarianceMatrix)
   {
      Component::finish(other);
   }

   // move
   ParameterCovariance(ParameterCovariance &&other) :
      NJOY_GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      label(this,std::move(other.label)),
      rowData(this,std::move(other.rowData)),
      parameterCovarianceMatrix(this,std::move(other.parameterCovarianceMatrix))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   ParameterCovariance &operator=(const ParameterCovariance &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         label = other.label;
         rowData = other.rowData;
         parameterCovarianceMatrix = other.parameterCovarianceMatrix;
      }
      return *this;
   }

   // move
   ParameterCovariance &operator=(ParameterCovariance &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         label = std::move(other.label);
         rowData = std::move(other.rowData);
         parameterCovarianceMatrix = std::move(other.parameterCovarianceMatrix);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDS/v2.0/general/ParameterCovariance/src/custom.hpp"
   #undef NJOY_GNDSTK_COMPONENT
}; // class ParameterCovariance

} // namespace general
} // namespace v2_0
} // namespace GNDS

#endif
