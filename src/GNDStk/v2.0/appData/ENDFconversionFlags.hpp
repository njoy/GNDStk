
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_APPDATA_ENDFCONVERSIONFLAGS
#define NJOY_GNDSTK_V2_0_APPDATA_ENDFCONVERSIONFLAGS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/appData/Conversion.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// appData::
// class ENDFconversionFlags
// -----------------------------------------------------------------------------

namespace appData {

class ENDFconversionFlags : public Component<ENDFconversionFlags> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "appData"; }
   static auto className() { return "ENDFconversionFlags"; }
   static auto GNDSName() { return "ENDFconversionFlags"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         std::optional<appData::Conversion>{}
            / --Child<>("conversion")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::optional<appData::Conversion> conversion;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // conversion
   const std::optional<appData::Conversion> &conversion() const
      { return content.conversion; }
   std::optional<appData::Conversion> &conversion()
      { return content.conversion; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // conversion(value)
   ENDFconversionFlags &conversion(const std::optional<appData::Conversion> &obj)
      { conversion() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   ENDFconversionFlags() :
      Component{
         BodyText{},
         content.conversion
      }
   {
      Component::finish();
   }

   // copy
   ENDFconversionFlags(const ENDFconversionFlags &other) :
      Component{
         other,
         content.conversion
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   ENDFconversionFlags(ENDFconversionFlags &&other) :
      Component{
         other,
         content.conversion
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   ENDFconversionFlags(const Node &node) :
      Component{
         BodyText{},
         content.conversion
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit ENDFconversionFlags(
      const std::optional<appData::Conversion> &conversion
   ) :
      Component{
         BodyText{},
         content.conversion
      },
      content{
         conversion
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   ENDFconversionFlags &operator=(const ENDFconversionFlags &) = default;

   // move
   ENDFconversionFlags &operator=(ENDFconversionFlags &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/appData/ENDFconversionFlags/src/custom.hpp"

}; // class ENDFconversionFlags

} // namespace appData
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
