
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_PROCESSED_ENERGYANGULARMC
#define NJOY_GNDSTK_V2_0_PROCESSED_ENERGYANGULARMC

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/ambiguousNamespace/Energy.hpp"
#include "GNDStk/v2.0/transport/EnergyAngular.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// processed::
// class EnergyAngularMC
// -----------------------------------------------------------------------------

namespace processed {

class EnergyAngularMC : public Component<EnergyAngularMC> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "processed"; }
   static auto className() { return "EnergyAngularMC"; }
   static auto GNDSName() { return "energyAngularMC"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("label") |
         XMLName{}
            / Meta<>("productFrame") |
         // children
         ambiguousNamespace::Energy{}
            / --Child<>("energy") |
         transport::EnergyAngular{}
            / --Child<>("energyAngular")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName label;
      XMLName productFrame;
      // children
      ambiguousNamespace::Energy energy;
      transport::EnergyAngular energyAngular;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // productFrame
   const XMLName &productFrame() const
      { return content.productFrame; }
   XMLName &productFrame()
      { return content.productFrame; }

   // energy
   const ambiguousNamespace::Energy &energy() const
      { return content.energy; }
   ambiguousNamespace::Energy &energy()
      { return content.energy; }

   // energyAngular
   const transport::EnergyAngular &energyAngular() const
      { return content.energyAngular; }
   transport::EnergyAngular &energyAngular()
      { return content.energyAngular; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // label(value)
   EnergyAngularMC &label(const XMLName &obj)
      { label() = obj; return *this; }

   // productFrame(value)
   EnergyAngularMC &productFrame(const XMLName &obj)
      { productFrame() = obj; return *this; }

   // energy(value)
   EnergyAngularMC &energy(const ambiguousNamespace::Energy &obj)
      { energy() = obj; return *this; }

   // energyAngular(value)
   EnergyAngularMC &energyAngular(const transport::EnergyAngular &obj)
      { energyAngular() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   EnergyAngularMC() :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.energy,
         content.energyAngular
      }
   {
      Component::finish();
   }

   // copy
   EnergyAngularMC(const EnergyAngularMC &other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.energy,
         content.energyAngular
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   EnergyAngularMC(EnergyAngularMC &&other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.energy,
         content.energyAngular
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   EnergyAngularMC(const Node &node) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.energy,
         content.energyAngular
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit EnergyAngularMC(
      const XMLName &label,
      const XMLName &productFrame,
      const ambiguousNamespace::Energy &energy,
      const transport::EnergyAngular &energyAngular
   ) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.energy,
         content.energyAngular
      },
      content{
         label,
         productFrame,
         energy,
         energyAngular
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   EnergyAngularMC &operator=(const EnergyAngularMC &) = default;

   // move
   EnergyAngularMC &operator=(EnergyAngularMC &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/processed/EnergyAngularMC/src/custom.hpp"

}; // class EnergyAngularMC

} // namespace processed
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
