
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_CONTAINERS_AXES
#define NJOY_GNDSTK_V2_0_CONTAINERS_AXES

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Axis.hpp"
#include "GNDStk/v2.0/containers/Grid.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// containers::
// class Axes
// -----------------------------------------------------------------------------

namespace containers {

class Axes : public Component<Axes> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "containers"; }
   static auto className() { return "Axes"; }
   static auto GNDSName() { return "axes"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<UTF8Text>{}
            / Meta<>("href") |
         // children
         std::optional<containers::Axis>{}
            / ++Child<>("axis") |
         std::optional<containers::Grid>{}
            / ++Child<>("grid")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<UTF8Text> href;
      // children
      std::optional<std::vector<containers::Axis>> axis;
      std::optional<std::vector<containers::Grid>> grid;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // href
   const std::optional<UTF8Text> &href() const
      { return content.href; }
   std::optional<UTF8Text> &href()
      { return content.href; }

   // axis
   const std::optional<std::vector<containers::Axis>> &axis() const
      { return content.axis; }
   std::optional<std::vector<containers::Axis>> &axis()
      { return content.axis; }

   // axis(index)
   const containers::Axis &axis(const std::size_t index) const
      { return getter(axis(), index, "axis"); }
   containers::Axis &axis(const std::size_t index)
      { return getter(axis(), index, "axis"); }

   // axis(label)
   const containers::Axis &axis(const std::string &label) const
      { return getter(axis(), label, "axis"); }
   containers::Axis &axis(const std::string &label)
      { return getter(axis(), label, "axis"); }

   // grid
   const std::optional<std::vector<containers::Grid>> &grid() const
      { return content.grid; }
   std::optional<std::vector<containers::Grid>> &grid()
      { return content.grid; }

   // grid(index)
   const containers::Grid &grid(const std::size_t index) const
      { return getter(grid(), index, "grid"); }
   containers::Grid &grid(const std::size_t index)
      { return getter(grid(), index, "grid"); }

   // grid(label)
   const containers::Grid &grid(const std::string &label) const
      { return getter(grid(), label, "grid"); }
   containers::Grid &grid(const std::string &label)
      { return getter(grid(), label, "grid"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // href(value)
   Axes &href(const std::optional<UTF8Text> &obj)
      { href() = obj; return *this; }

   // axis(value)
   Axes &axis(const std::optional<std::vector<containers::Axis>> &obj)
      { axis() = obj; return *this; }

   // axis(index,value)
   Axes &axis(
      const std::size_t index,
      const containers::Axis &obj
   ) {
      axis(index) = obj; return *this;
   }

   // axis(label,value)
   Axes &axis(
      const std::string &label,
      const containers::Axis &obj
   ) {
      axis(label) = obj; return *this;
   }

   // grid(value)
   Axes &grid(const std::optional<std::vector<containers::Grid>> &obj)
      { grid() = obj; return *this; }

   // grid(index,value)
   Axes &grid(
      const std::size_t index,
      const containers::Grid &obj
   ) {
      grid(index) = obj; return *this;
   }

   // grid(label,value)
   Axes &grid(
      const std::string &label,
      const containers::Grid &obj
   ) {
      grid(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Axes() :
      Component{
         BodyText{},
         content.href,
         content.axis,
         content.grid
      }
   {
      Component::finish();
   }

   // copy
   Axes(const Axes &other) :
      Component{
         other,
         content.href,
         content.axis,
         content.grid
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Axes(Axes &&other) :
      Component{
         other,
         content.href,
         content.axis,
         content.grid
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Axes(const Node &node) :
      Component{
         BodyText{},
         content.href,
         content.axis,
         content.grid
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Axes(
      const std::optional<UTF8Text> &href,
      const std::optional<std::vector<containers::Axis>> &axis,
      const std::optional<std::vector<containers::Grid>> &grid
   ) :
      Component{
         BodyText{},
         content.href,
         content.axis,
         content.grid
      },
      content{
         href,
         axis,
         grid
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Axes &operator=(const Axes &) = default;

   // move
   Axes &operator=(Axes &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/containers/Axes/src/custom.hpp"

}; // class Axes

} // namespace containers
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
