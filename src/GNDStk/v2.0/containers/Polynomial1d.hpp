
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_CONTAINERS_POLYNOMIAL1D
#define NJOY_GNDSTK_V2_0_CONTAINERS_POLYNOMIAL1D

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Axes.hpp"
#include "GNDStk/v2.0/containers/Values.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// containers::
// class Polynomial1d
// -----------------------------------------------------------------------------

namespace containers {

class Polynomial1d : public Component<Polynomial1d> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "containers"; }
   static auto className() { return "Polynomial1d"; }
   static auto GNDSName() { return "polynomial1d"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<Float64>{}
            / Meta<>("domainMax") |
         std::optional<Float64>{}
            / Meta<>("domainMin") |
         std::optional<XMLName>{}
            / Meta<>("label") |
         Defaulted<Integer32>{0}
            / Meta<>("lowerIndex") |
         std::optional<Float64>{}
            / Meta<>("outerDomainValue") |
         // children
         containers::Axes{}
            / --Child<>("axes") |
         containers::Values{}
            / --Child<>("values")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
      static inline const Integer32 lowerIndex = 0;
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<Float64> domainMax;
      std::optional<Float64> domainMin;
      std::optional<XMLName> label;
      Defaulted<Integer32> lowerIndex{0};
      std::optional<Float64> outerDomainValue;
      // children
      containers::Axes axes;
      containers::Values values;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // domainMax
   const std::optional<Float64> &domainMax() const
      { return content.domainMax; }
   std::optional<Float64> &domainMax()
      { return content.domainMax; }

   // domainMin
   const std::optional<Float64> &domainMin() const
      { return content.domainMin; }
   std::optional<Float64> &domainMin()
      { return content.domainMin; }

   // label
   const std::optional<XMLName> &label() const
      { return content.label; }
   std::optional<XMLName> &label()
      { return content.label; }

   // lowerIndex
   const Defaulted<Integer32> &lowerIndex() const
      { return content.lowerIndex; }
   Defaulted<Integer32> &lowerIndex()
      { return content.lowerIndex; }

   // outerDomainValue
   const std::optional<Float64> &outerDomainValue() const
      { return content.outerDomainValue; }
   std::optional<Float64> &outerDomainValue()
      { return content.outerDomainValue; }

   // axes
   const containers::Axes &axes() const
      { return content.axes; }
   containers::Axes &axes()
      { return content.axes; }

   // values
   const containers::Values &values() const
      { return content.values; }
   containers::Values &values()
      { return content.values; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // domainMax(value)
   Polynomial1d &domainMax(const std::optional<Float64> &obj)
      { domainMax() = obj; return *this; }

   // domainMin(value)
   Polynomial1d &domainMin(const std::optional<Float64> &obj)
      { domainMin() = obj; return *this; }

   // label(value)
   Polynomial1d &label(const std::optional<XMLName> &obj)
      { label() = obj; return *this; }

   // lowerIndex(value)
   Polynomial1d &lowerIndex(const Defaulted<Integer32> &obj)
      { content.lowerIndex = obj; return *this; }
   Polynomial1d &lowerIndex(const std::optional<Integer32> &obj)
      { content.lowerIndex = obj; return *this; }

   // outerDomainValue(value)
   Polynomial1d &outerDomainValue(const std::optional<Float64> &obj)
      { outerDomainValue() = obj; return *this; }

   // axes(value)
   Polynomial1d &axes(const containers::Axes &obj)
      { axes() = obj; return *this; }

   // values(value)
   Polynomial1d &values(const containers::Values &obj)
      { values() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Polynomial1d() :
      Component{
         BodyText{},
         content.domainMax,
         content.domainMin,
         content.label,
         content.lowerIndex,
         content.outerDomainValue,
         content.axes,
         content.values
      }
   {
      Component::finish();
   }

   // copy
   Polynomial1d(const Polynomial1d &other) :
      Component{
         other,
         content.domainMax,
         content.domainMin,
         content.label,
         content.lowerIndex,
         content.outerDomainValue,
         content.axes,
         content.values
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Polynomial1d(Polynomial1d &&other) :
      Component{
         other,
         content.domainMax,
         content.domainMin,
         content.label,
         content.lowerIndex,
         content.outerDomainValue,
         content.axes,
         content.values
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Polynomial1d(const Node &node) :
      Component{
         BodyText{},
         content.domainMax,
         content.domainMin,
         content.label,
         content.lowerIndex,
         content.outerDomainValue,
         content.axes,
         content.values
      }
   {
      Component::finish(node);
   }

   // from fields
   // std::optional replaces Defaulted; this class knows the default(s)
   explicit Polynomial1d(
      const std::optional<Float64> &domainMax,
      const std::optional<Float64> &domainMin,
      const std::optional<XMLName> &label,
      const std::optional<Integer32> &lowerIndex,
      const std::optional<Float64> &outerDomainValue,
      const containers::Axes &axes,
      const containers::Values &values
   ) :
      Component{
         BodyText{},
         content.domainMax,
         content.domainMin,
         content.label,
         content.lowerIndex,
         content.outerDomainValue,
         content.axes,
         content.values
      },
      content{
         domainMax,
         domainMin,
         label,
         Defaulted<Integer32>(defaults.lowerIndex,lowerIndex),
         outerDomainValue,
         axes,
         values
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Polynomial1d &operator=(const Polynomial1d &) = default;

   // move
   Polynomial1d &operator=(Polynomial1d &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/containers/Polynomial1d/src/custom.hpp"

}; // class Polynomial1d

} // namespace containers
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
