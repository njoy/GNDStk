
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_CONTAINERS_COLUMNHEADERS
#define NJOY_GNDSTK_V2_0_CONTAINERS_COLUMNHEADERS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Column.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// containers::
// class ColumnHeaders
// -----------------------------------------------------------------------------

namespace containers {

class ColumnHeaders : public Component<ColumnHeaders> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "containers"; }
   static auto className() { return "ColumnHeaders"; }
   static auto GNDSName() { return "columnHeaders"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         containers::Column{}
            / ++Child<>("column")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<containers::Column> column;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // column
   const std::vector<containers::Column> &column() const
      { return content.column; }
   std::vector<containers::Column> &column()
      { return content.column; }

   // column(index)
   const containers::Column &column(const std::size_t index) const
      { return getter(column(), index, "column"); }
   containers::Column &column(const std::size_t index)
      { return getter(column(), index, "column"); }

   // column(label)
   const containers::Column &column(const std::string &label) const
      { return getter(column(), label, "column"); }
   containers::Column &column(const std::string &label)
      { return getter(column(), label, "column"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // column(value)
   ColumnHeaders &column(const std::vector<containers::Column> &obj)
      { column() = obj; return *this; }

   // column(index,value)
   ColumnHeaders &column(
      const std::size_t index,
      const containers::Column &obj
   ) {
      column(index) = obj; return *this;
   }

   // column(label,value)
   ColumnHeaders &column(
      const std::string &label,
      const containers::Column &obj
   ) {
      column(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   ColumnHeaders() :
      Component{
         BodyText{},
         content.column
      }
   {
      Component::finish();
   }

   // copy
   ColumnHeaders(const ColumnHeaders &other) :
      Component{
         other,
         content.column
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   ColumnHeaders(ColumnHeaders &&other) :
      Component{
         other,
         content.column
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   ColumnHeaders(const Node &node) :
      Component{
         BodyText{},
         content.column
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit ColumnHeaders(
      const std::vector<containers::Column> &column
   ) :
      Component{
         BodyText{},
         content.column
      },
      content{
         column
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   ColumnHeaders &operator=(const ColumnHeaders &) = default;

   // move
   ColumnHeaders &operator=(ColumnHeaders &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/containers/ColumnHeaders/src/custom.hpp"

}; // class ColumnHeaders

} // namespace containers
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
