
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_CONTAINERS_GRIDDED2D
#define NJOY_GNDSTK_V2_0_CONTAINERS_GRIDDED2D

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Array.hpp"
#include "GNDStk/v2.0/containers/Axes.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// containers::
// class Gridded2d
// -----------------------------------------------------------------------------

namespace containers {

class Gridded2d : public Component<Gridded2d> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "containers"; }
   static auto className() { return "Gridded2d"; }
   static auto GNDSName() { return "gridded2d"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<XMLName>{}
            / Meta<>("label") |
         // children
         containers::Array{}
            / --Child<>("array") |
         containers::Axes{}
            / --Child<>("axes")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<XMLName> label;
      // children
      containers::Array array;
      containers::Axes axes;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // label
   const std::optional<XMLName> &label() const
      { return content.label; }
   std::optional<XMLName> &label()
      { return content.label; }

   // array
   const containers::Array &array() const
      { return content.array; }
   containers::Array &array()
      { return content.array; }

   // axes
   const containers::Axes &axes() const
      { return content.axes; }
   containers::Axes &axes()
      { return content.axes; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // label(value)
   Gridded2d &label(const std::optional<XMLName> &obj)
      { label() = obj; return *this; }

   // array(value)
   Gridded2d &array(const containers::Array &obj)
      { array() = obj; return *this; }

   // axes(value)
   Gridded2d &axes(const containers::Axes &obj)
      { axes() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Gridded2d() :
      Component{
         BodyText{},
         content.label,
         content.array,
         content.axes
      }
   {
      Component::finish();
   }

   // copy
   Gridded2d(const Gridded2d &other) :
      Component{
         other,
         content.label,
         content.array,
         content.axes
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Gridded2d(Gridded2d &&other) :
      Component{
         other,
         content.label,
         content.array,
         content.axes
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Gridded2d(const Node &node) :
      Component{
         BodyText{},
         content.label,
         content.array,
         content.axes
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Gridded2d(
      const std::optional<XMLName> &label,
      const containers::Array &array,
      const containers::Axes &axes
   ) :
      Component{
         BodyText{},
         content.label,
         content.array,
         content.axes
      },
      content{
         label,
         array,
         axes
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Gridded2d &operator=(const Gridded2d &) = default;

   // move
   Gridded2d &operator=(Gridded2d &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/containers/Gridded2d/src/custom.hpp"

}; // class Gridded2d

} // namespace containers
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
