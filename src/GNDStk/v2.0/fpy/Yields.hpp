
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_FPY_YIELDS
#define NJOY_GNDSTK_V2_0_FPY_YIELDS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/fpy/Nuclides.hpp"
#include "GNDStk/v2.0/containers/Values.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// fpy::
// class Yields
// -----------------------------------------------------------------------------

namespace fpy {

class Yields : public Component<Yields> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "fpy"; }
   static auto className() { return "Yields"; }
   static auto GNDSName() { return "yields"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         fpy::Nuclides{}
            / --Child<>("nuclides") |
         containers::Values{}
            / --Child<>("values")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      fpy::Nuclides nuclides;
      containers::Values values;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // nuclides
   const fpy::Nuclides &nuclides() const
      { return content.nuclides; }
   fpy::Nuclides &nuclides()
      { return content.nuclides; }

   // values
   const containers::Values &values() const
      { return content.values; }
   containers::Values &values()
      { return content.values; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // nuclides(value)
   Yields &nuclides(const fpy::Nuclides &obj)
      { nuclides() = obj; return *this; }

   // values(value)
   Yields &values(const containers::Values &obj)
      { values() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Yields() :
      Component{
         BodyText{},
         content.nuclides,
         content.values
      }
   {
      Component::finish();
   }

   // copy
   Yields(const Yields &other) :
      Component{
         other,
         content.nuclides,
         content.values
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Yields(Yields &&other) :
      Component{
         other,
         content.nuclides,
         content.values
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Yields(const Node &node) :
      Component{
         BodyText{},
         content.nuclides,
         content.values
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Yields(
      const fpy::Nuclides &nuclides,
      const containers::Values &values
   ) :
      Component{
         BodyText{},
         content.nuclides,
         content.values
      },
      content{
         nuclides,
         values
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Yields &operator=(const Yields &) = default;

   // move
   Yields &operator=(Yields &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/fpy/Yields/src/custom.hpp"

}; // class Yields

} // namespace fpy
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
