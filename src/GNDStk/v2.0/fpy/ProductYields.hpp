
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_FPY_PRODUCTYIELDS
#define NJOY_GNDSTK_V2_0_FPY_PRODUCTYIELDS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/fpy/ProductYield.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// fpy::
// class ProductYields
// -----------------------------------------------------------------------------

namespace fpy {

class ProductYields : public Component<ProductYields> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "fpy"; }
   static auto className() { return "ProductYields"; }
   static auto GNDSName() { return "productYields"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         fpy::ProductYield{}
            / ++Child<>("productYield")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<fpy::ProductYield> productYield;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // productYield
   const std::vector<fpy::ProductYield> &productYield() const
      { return content.productYield; }
   std::vector<fpy::ProductYield> &productYield()
      { return content.productYield; }

   // productYield(index)
   const fpy::ProductYield &productYield(const std::size_t index) const
      { return getter(productYield(), index, "productYield"); }
   fpy::ProductYield &productYield(const std::size_t index)
      { return getter(productYield(), index, "productYield"); }

   // productYield(label)
   const fpy::ProductYield &productYield(const std::string &label) const
      { return getter(productYield(), label, "productYield"); }
   fpy::ProductYield &productYield(const std::string &label)
      { return getter(productYield(), label, "productYield"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // productYield(value)
   ProductYields &productYield(const std::vector<fpy::ProductYield> &obj)
      { productYield() = obj; return *this; }

   // productYield(index,value)
   ProductYields &productYield(
      const std::size_t index,
      const fpy::ProductYield &obj
   ) {
      productYield(index) = obj; return *this;
   }

   // productYield(label,value)
   ProductYields &productYield(
      const std::string &label,
      const fpy::ProductYield &obj
   ) {
      productYield(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   ProductYields() :
      Component{
         BodyText{},
         content.productYield
      }
   {
      Component::finish();
   }

   // copy
   ProductYields(const ProductYields &other) :
      Component{
         other,
         content.productYield
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   ProductYields(ProductYields &&other) :
      Component{
         other,
         content.productYield
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   ProductYields(const Node &node) :
      Component{
         BodyText{},
         content.productYield
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit ProductYields(
      const std::vector<fpy::ProductYield> &productYield
   ) :
      Component{
         BodyText{},
         content.productYield
      },
      content{
         productYield
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   ProductYields &operator=(const ProductYields &) = default;

   // move
   ProductYields &operator=(ProductYields &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/fpy/ProductYields/src/custom.hpp"

}; // class ProductYields

} // namespace fpy
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
