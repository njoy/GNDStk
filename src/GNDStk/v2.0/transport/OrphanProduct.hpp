
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_TRANSPORT_ORPHANPRODUCT
#define NJOY_GNDSTK_V2_0_TRANSPORT_ORPHANPRODUCT

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/transport/CrossSection.hpp"
#include "GNDStk/v2.0/transport/OutputChannel.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// transport::
// class OrphanProduct
// -----------------------------------------------------------------------------

namespace transport {

class OrphanProduct : public Component<OrphanProduct> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "transport"; }
   static auto className() { return "OrphanProduct"; }
   static auto GNDSName() { return "orphanProduct"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         Integer32{}
            / Meta<>("ENDF_MT") |
         XMLName{}
            / Meta<>("label") |
         // children
         transport::CrossSection{}
            / --Child<>("crossSection") |
         transport::OutputChannel{}
            / --Child<>("outputChannel")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      Integer32 ENDF_MT;
      XMLName label;
      // children
      transport::CrossSection crossSection;
      transport::OutputChannel outputChannel;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // ENDF_MT
   const Integer32 &ENDF_MT() const
      { return content.ENDF_MT; }
   Integer32 &ENDF_MT()
      { return content.ENDF_MT; }

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // crossSection
   const transport::CrossSection &crossSection() const
      { return content.crossSection; }
   transport::CrossSection &crossSection()
      { return content.crossSection; }

   // outputChannel
   const transport::OutputChannel &outputChannel() const
      { return content.outputChannel; }
   transport::OutputChannel &outputChannel()
      { return content.outputChannel; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // ENDF_MT(value)
   OrphanProduct &ENDF_MT(const Integer32 &obj)
      { ENDF_MT() = obj; return *this; }

   // label(value)
   OrphanProduct &label(const XMLName &obj)
      { label() = obj; return *this; }

   // crossSection(value)
   OrphanProduct &crossSection(const transport::CrossSection &obj)
      { crossSection() = obj; return *this; }

   // outputChannel(value)
   OrphanProduct &outputChannel(const transport::OutputChannel &obj)
      { outputChannel() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   OrphanProduct() :
      Component{
         BodyText{},
         content.ENDF_MT,
         content.label,
         content.crossSection,
         content.outputChannel
      }
   {
      Component::finish();
   }

   // copy
   OrphanProduct(const OrphanProduct &other) :
      Component{
         other,
         content.ENDF_MT,
         content.label,
         content.crossSection,
         content.outputChannel
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   OrphanProduct(OrphanProduct &&other) :
      Component{
         other,
         content.ENDF_MT,
         content.label,
         content.crossSection,
         content.outputChannel
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   OrphanProduct(const Node &node) :
      Component{
         BodyText{},
         content.ENDF_MT,
         content.label,
         content.crossSection,
         content.outputChannel
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit OrphanProduct(
      const Integer32 &ENDF_MT,
      const XMLName &label,
      const transport::CrossSection &crossSection,
      const transport::OutputChannel &outputChannel
   ) :
      Component{
         BodyText{},
         content.ENDF_MT,
         content.label,
         content.crossSection,
         content.outputChannel
      },
      content{
         ENDF_MT,
         label,
         crossSection,
         outputChannel
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   OrphanProduct &operator=(const OrphanProduct &) = default;

   // move
   OrphanProduct &operator=(OrphanProduct &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/transport/OrphanProduct/src/custom.hpp"

}; // class OrphanProduct

} // namespace transport
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
