
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_TRANSPORT_ENERGYANGULAR
#define NJOY_GNDSTK_V2_0_TRANSPORT_ENERGYANGULAR

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/XYs3d.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// transport::
// class EnergyAngular
// -----------------------------------------------------------------------------

namespace transport {

class EnergyAngular : public Component<EnergyAngular> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "transport"; }
   static auto className() { return "EnergyAngular"; }
   static auto GNDSName() { return "energyAngular"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<XMLName>{}
            / Meta<>("label") |
         XMLName{}
            / Meta<>("productFrame") |
         // children
         containers::XYs3d{}
            / --Child<>("XYs3d")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<XMLName> label;
      XMLName productFrame;
      // children
      containers::XYs3d XYs3d;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // label
   const std::optional<XMLName> &label() const
      { return content.label; }
   std::optional<XMLName> &label()
      { return content.label; }

   // productFrame
   const XMLName &productFrame() const
      { return content.productFrame; }
   XMLName &productFrame()
      { return content.productFrame; }

   // XYs3d
   const containers::XYs3d &XYs3d() const
      { return content.XYs3d; }
   containers::XYs3d &XYs3d()
      { return content.XYs3d; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // label(value)
   EnergyAngular &label(const std::optional<XMLName> &obj)
      { label() = obj; return *this; }

   // productFrame(value)
   EnergyAngular &productFrame(const XMLName &obj)
      { productFrame() = obj; return *this; }

   // XYs3d(value)
   EnergyAngular &XYs3d(const containers::XYs3d &obj)
      { XYs3d() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   EnergyAngular() :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.XYs3d
      }
   {
      Component::finish();
   }

   // copy
   EnergyAngular(const EnergyAngular &other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.XYs3d
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   EnergyAngular(EnergyAngular &&other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.XYs3d
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   EnergyAngular(const Node &node) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.XYs3d
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit EnergyAngular(
      const std::optional<XMLName> &label,
      const XMLName &productFrame,
      const containers::XYs3d &XYs3d
   ) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.XYs3d
      },
      content{
         label,
         productFrame,
         XYs3d
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   EnergyAngular &operator=(const EnergyAngular &) = default;

   // move
   EnergyAngular &operator=(EnergyAngular &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/transport/EnergyAngular/src/custom.hpp"

}; // class EnergyAngular

} // namespace transport
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
