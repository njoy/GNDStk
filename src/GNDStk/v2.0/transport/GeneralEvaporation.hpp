
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_TRANSPORT_GENERALEVAPORATION
#define NJOY_GNDSTK_V2_0_TRANSPORT_GENERALEVAPORATION

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/transport/U.hpp"
#include "GNDStk/v2.0/transport/G.hpp"
#include "GNDStk/v2.0/transport/Theta.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// transport::
// class GeneralEvaporation
// -----------------------------------------------------------------------------

namespace transport {

class GeneralEvaporation : public Component<GeneralEvaporation> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "transport"; }
   static auto className() { return "GeneralEvaporation"; }
   static auto GNDSName() { return "generalEvaporation"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         std::optional<transport::U>{}
            / --Child<>("U") |
         std::optional<transport::G>{}
            / --Child<>("g") |
         std::optional<transport::Theta>{}
            / --Child<>("theta")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::optional<transport::U> U;
      std::optional<transport::G> g;
      std::optional<transport::Theta> theta;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // U
   const std::optional<transport::U> &U() const
      { return content.U; }
   std::optional<transport::U> &U()
      { return content.U; }

   // g
   const std::optional<transport::G> &g() const
      { return content.g; }
   std::optional<transport::G> &g()
      { return content.g; }

   // theta
   const std::optional<transport::Theta> &theta() const
      { return content.theta; }
   std::optional<transport::Theta> &theta()
      { return content.theta; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // U(value)
   GeneralEvaporation &U(const std::optional<transport::U> &obj)
      { U() = obj; return *this; }

   // g(value)
   GeneralEvaporation &g(const std::optional<transport::G> &obj)
      { g() = obj; return *this; }

   // theta(value)
   GeneralEvaporation &theta(const std::optional<transport::Theta> &obj)
      { theta() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   GeneralEvaporation() :
      Component{
         BodyText{},
         content.U,
         content.g,
         content.theta
      }
   {
      Component::finish();
   }

   // copy
   GeneralEvaporation(const GeneralEvaporation &other) :
      Component{
         other,
         content.U,
         content.g,
         content.theta
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   GeneralEvaporation(GeneralEvaporation &&other) :
      Component{
         other,
         content.U,
         content.g,
         content.theta
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   GeneralEvaporation(const Node &node) :
      Component{
         BodyText{},
         content.U,
         content.g,
         content.theta
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit GeneralEvaporation(
      const std::optional<transport::U> &U,
      const std::optional<transport::G> &g,
      const std::optional<transport::Theta> &theta
   ) :
      Component{
         BodyText{},
         content.U,
         content.g,
         content.theta
      },
      content{
         U,
         g,
         theta
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   GeneralEvaporation &operator=(const GeneralEvaporation &) = default;

   // move
   GeneralEvaporation &operator=(GeneralEvaporation &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/transport/GeneralEvaporation/src/custom.hpp"

}; // class GeneralEvaporation

} // namespace transport
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
