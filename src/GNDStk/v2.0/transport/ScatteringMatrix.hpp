
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_TRANSPORT_SCATTERINGMATRIX
#define NJOY_GNDSTK_V2_0_TRANSPORT_SCATTERINGMATRIX

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Gridded3d.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// transport::
// class ScatteringMatrix
// -----------------------------------------------------------------------------

namespace transport {

class ScatteringMatrix : public Component<ScatteringMatrix> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "transport"; }
   static auto className() { return "ScatteringMatrix"; }
   static auto GNDSName() { return "scatteringMatrix"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("label") |
         XMLName{}
            / Meta<>("productFrame") |
         // children
         containers::Gridded3d{}
            / --Child<>("gridded3d")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName label;
      XMLName productFrame;
      // children
      containers::Gridded3d gridded3d;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // productFrame
   const XMLName &productFrame() const
      { return content.productFrame; }
   XMLName &productFrame()
      { return content.productFrame; }

   // gridded3d
   const containers::Gridded3d &gridded3d() const
      { return content.gridded3d; }
   containers::Gridded3d &gridded3d()
      { return content.gridded3d; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // label(value)
   ScatteringMatrix &label(const XMLName &obj)
      { label() = obj; return *this; }

   // productFrame(value)
   ScatteringMatrix &productFrame(const XMLName &obj)
      { productFrame() = obj; return *this; }

   // gridded3d(value)
   ScatteringMatrix &gridded3d(const containers::Gridded3d &obj)
      { gridded3d() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   ScatteringMatrix() :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.gridded3d
      }
   {
      Component::finish();
   }

   // copy
   ScatteringMatrix(const ScatteringMatrix &other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.gridded3d
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   ScatteringMatrix(ScatteringMatrix &&other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.gridded3d
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   ScatteringMatrix(const Node &node) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.gridded3d
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit ScatteringMatrix(
      const XMLName &label,
      const XMLName &productFrame,
      const containers::Gridded3d &gridded3d
   ) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.gridded3d
      },
      content{
         label,
         productFrame,
         gridded3d
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   ScatteringMatrix &operator=(const ScatteringMatrix &) = default;

   // move
   ScatteringMatrix &operator=(ScatteringMatrix &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/transport/ScatteringMatrix/src/custom.hpp"

}; // class ScatteringMatrix

} // namespace transport
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
