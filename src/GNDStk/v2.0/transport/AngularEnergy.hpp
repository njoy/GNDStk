
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_TRANSPORT_ANGULARENERGY
#define NJOY_GNDSTK_V2_0_TRANSPORT_ANGULARENERGY

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/XYs3d.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// transport::
// class AngularEnergy
// -----------------------------------------------------------------------------

namespace transport {

class AngularEnergy : public Component<AngularEnergy> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "transport"; }
   static auto className() { return "AngularEnergy"; }
   static auto GNDSName() { return "angularEnergy"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("label") |
         XMLName{}
            / Meta<>("productFrame") |
         // children
         containers::XYs3d{}
            / --Child<>("XYs3d")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName label;
      XMLName productFrame;
      // children
      containers::XYs3d XYs3d;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // productFrame
   const XMLName &productFrame() const
      { return content.productFrame; }
   XMLName &productFrame()
      { return content.productFrame; }

   // XYs3d
   const containers::XYs3d &XYs3d() const
      { return content.XYs3d; }
   containers::XYs3d &XYs3d()
      { return content.XYs3d; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // label(value)
   AngularEnergy &label(const XMLName &obj)
      { label() = obj; return *this; }

   // productFrame(value)
   AngularEnergy &productFrame(const XMLName &obj)
      { productFrame() = obj; return *this; }

   // XYs3d(value)
   AngularEnergy &XYs3d(const containers::XYs3d &obj)
      { XYs3d() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   AngularEnergy() :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.XYs3d
      }
   {
      Component::finish();
   }

   // copy
   AngularEnergy(const AngularEnergy &other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.XYs3d
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   AngularEnergy(AngularEnergy &&other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.XYs3d
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   AngularEnergy(const Node &node) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.XYs3d
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit AngularEnergy(
      const XMLName &label,
      const XMLName &productFrame,
      const containers::XYs3d &XYs3d
   ) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.XYs3d
      },
      content{
         label,
         productFrame,
         XYs3d
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   AngularEnergy &operator=(const AngularEnergy &) = default;

   // move
   AngularEnergy &operator=(AngularEnergy &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/transport/AngularEnergy/src/custom.hpp"

}; // class AngularEnergy

} // namespace transport
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
