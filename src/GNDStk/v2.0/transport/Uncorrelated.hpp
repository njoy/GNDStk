
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_TRANSPORT_UNCORRELATED
#define NJOY_GNDSTK_V2_0_TRANSPORT_UNCORRELATED

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/transport/Angular.hpp"
#include "GNDStk/v2.0/transport/Energy.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// transport::
// class Uncorrelated
// -----------------------------------------------------------------------------

namespace transport {

class Uncorrelated : public Component<Uncorrelated> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "transport"; }
   static auto className() { return "Uncorrelated"; }
   static auto GNDSName() { return "uncorrelated"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<XMLName>{}
            / Meta<>("label") |
         XMLName{}
            / Meta<>("productFrame") |
         // children
         transport::Angular{}
            / --Child<>("angular") |
         transport::Energy{}
            / --Child<>("energy")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<XMLName> label;
      XMLName productFrame;
      // children
      transport::Angular angular;
      transport::Energy energy;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // label
   const std::optional<XMLName> &label() const
      { return content.label; }
   std::optional<XMLName> &label()
      { return content.label; }

   // productFrame
   const XMLName &productFrame() const
      { return content.productFrame; }
   XMLName &productFrame()
      { return content.productFrame; }

   // angular
   const transport::Angular &angular() const
      { return content.angular; }
   transport::Angular &angular()
      { return content.angular; }

   // energy
   const transport::Energy &energy() const
      { return content.energy; }
   transport::Energy &energy()
      { return content.energy; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // label(value)
   Uncorrelated &label(const std::optional<XMLName> &obj)
      { label() = obj; return *this; }

   // productFrame(value)
   Uncorrelated &productFrame(const XMLName &obj)
      { productFrame() = obj; return *this; }

   // angular(value)
   Uncorrelated &angular(const transport::Angular &obj)
      { angular() = obj; return *this; }

   // energy(value)
   Uncorrelated &energy(const transport::Energy &obj)
      { energy() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Uncorrelated() :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.angular,
         content.energy
      }
   {
      Component::finish();
   }

   // copy
   Uncorrelated(const Uncorrelated &other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.angular,
         content.energy
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Uncorrelated(Uncorrelated &&other) :
      Component{
         other,
         content.label,
         content.productFrame,
         content.angular,
         content.energy
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Uncorrelated(const Node &node) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.angular,
         content.energy
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Uncorrelated(
      const std::optional<XMLName> &label,
      const XMLName &productFrame,
      const transport::Angular &angular,
      const transport::Energy &energy
   ) :
      Component{
         BodyText{},
         content.label,
         content.productFrame,
         content.angular,
         content.energy
      },
      content{
         label,
         productFrame,
         angular,
         energy
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Uncorrelated &operator=(const Uncorrelated &) = default;

   // move
   Uncorrelated &operator=(Uncorrelated &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/transport/Uncorrelated/src/custom.hpp"

}; // class Uncorrelated

} // namespace transport
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
