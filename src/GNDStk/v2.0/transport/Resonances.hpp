
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_TRANSPORT_RESONANCES
#define NJOY_GNDSTK_V2_0_TRANSPORT_RESONANCES

// core interface
#include "GNDStk.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// transport::
// class Resonances
// -----------------------------------------------------------------------------

namespace transport {

class Resonances : public Component<Resonances> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "transport"; }
   static auto className() { return "Resonances"; }
   static auto GNDSName() { return "resonances"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::string{}
            / Meta<>("href") |
         std::optional<XMLName>{}
            / Meta<>("label")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::string href;
      std::optional<XMLName> label;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // href
   const std::string &href() const
      { return content.href; }
   std::string &href()
      { return content.href; }

   // label
   const std::optional<XMLName> &label() const
      { return content.label; }
   std::optional<XMLName> &label()
      { return content.label; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // href(value)
   Resonances &href(const std::string &obj)
      { href() = obj; return *this; }

   // label(value)
   Resonances &label(const std::optional<XMLName> &obj)
      { label() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Resonances() :
      Component{
         BodyText{},
         content.href,
         content.label
      }
   {
      Component::finish();
   }

   // copy
   Resonances(const Resonances &other) :
      Component{
         other,
         content.href,
         content.label
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Resonances(Resonances &&other) :
      Component{
         other,
         content.href,
         content.label
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Resonances(const Node &node) :
      Component{
         BodyText{},
         content.href,
         content.label
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Resonances(
      const std::string &href,
      const std::optional<XMLName> &label
   ) :
      Component{
         BodyText{},
         content.href,
         content.label
      },
      content{
         href,
         label
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Resonances &operator=(const Resonances &) = default;

   // move
   Resonances &operator=(Resonances &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/transport/Resonances/src/custom.hpp"

}; // class Resonances

} // namespace transport
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
