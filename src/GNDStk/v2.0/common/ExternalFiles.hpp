
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_COMMON_EXTERNALFILES
#define NJOY_GNDSTK_V2_0_COMMON_EXTERNALFILES

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/common/ExternalFile.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// common::
// class ExternalFiles
// -----------------------------------------------------------------------------

namespace common {

class ExternalFiles : public Component<ExternalFiles> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "common"; }
   static auto className() { return "ExternalFiles"; }
   static auto GNDSName() { return "externalFiles"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         common::ExternalFile{}
            / ++Child<>("externalFile")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<common::ExternalFile> externalFile;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // externalFile
   const std::vector<common::ExternalFile> &externalFile() const
      { return content.externalFile; }
   std::vector<common::ExternalFile> &externalFile()
      { return content.externalFile; }

   // externalFile(index)
   const common::ExternalFile &externalFile(const std::size_t index) const
      { return getter(externalFile(), index, "externalFile"); }
   common::ExternalFile &externalFile(const std::size_t index)
      { return getter(externalFile(), index, "externalFile"); }

   // externalFile(label)
   const common::ExternalFile &externalFile(const std::string &label) const
      { return getter(externalFile(), label, "externalFile"); }
   common::ExternalFile &externalFile(const std::string &label)
      { return getter(externalFile(), label, "externalFile"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // externalFile(value)
   ExternalFiles &externalFile(const std::vector<common::ExternalFile> &obj)
      { externalFile() = obj; return *this; }

   // externalFile(index,value)
   ExternalFiles &externalFile(
      const std::size_t index,
      const common::ExternalFile &obj
   ) {
      externalFile(index) = obj; return *this;
   }

   // externalFile(label,value)
   ExternalFiles &externalFile(
      const std::string &label,
      const common::ExternalFile &obj
   ) {
      externalFile(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   ExternalFiles() :
      Component{
         BodyText{},
         content.externalFile
      }
   {
      Component::finish();
   }

   // copy
   ExternalFiles(const ExternalFiles &other) :
      Component{
         other,
         content.externalFile
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   ExternalFiles(ExternalFiles &&other) :
      Component{
         other,
         content.externalFile
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   ExternalFiles(const Node &node) :
      Component{
         BodyText{},
         content.externalFile
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit ExternalFiles(
      const std::vector<common::ExternalFile> &externalFile
   ) :
      Component{
         BodyText{},
         content.externalFile
      },
      content{
         externalFile
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   ExternalFiles &operator=(const ExternalFiles &) = default;

   // move
   ExternalFiles &operator=(ExternalFiles &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/common/ExternalFiles/src/custom.hpp"

}; // class ExternalFiles

} // namespace common
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
