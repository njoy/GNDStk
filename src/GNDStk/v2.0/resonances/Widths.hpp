
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_RESONANCES_WIDTHS
#define NJOY_GNDSTK_V2_0_RESONANCES_WIDTHS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/resonances/Width.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// resonances::
// class Widths
// -----------------------------------------------------------------------------

namespace resonances {

class Widths : public Component<Widths> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "resonances"; }
   static auto className() { return "Widths"; }
   static auto GNDSName() { return "widths"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         resonances::Width{}
            / ++Child<>("width")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<resonances::Width> width;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // width
   const std::vector<resonances::Width> &width() const
      { return content.width; }
   std::vector<resonances::Width> &width()
      { return content.width; }

   // width(index)
   const resonances::Width &width(const std::size_t index) const
      { return getter(width(), index, "width"); }
   resonances::Width &width(const std::size_t index)
      { return getter(width(), index, "width"); }

   // width(label)
   const resonances::Width &width(const std::string &label) const
      { return getter(width(), label, "width"); }
   resonances::Width &width(const std::string &label)
      { return getter(width(), label, "width"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // width(value)
   Widths &width(const std::vector<resonances::Width> &obj)
      { width() = obj; return *this; }

   // width(index,value)
   Widths &width(
      const std::size_t index,
      const resonances::Width &obj
   ) {
      width(index) = obj; return *this;
   }

   // width(label,value)
   Widths &width(
      const std::string &label,
      const resonances::Width &obj
   ) {
      width(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Widths() :
      Component{
         BodyText{},
         content.width
      }
   {
      Component::finish();
   }

   // copy
   Widths(const Widths &other) :
      Component{
         other,
         content.width
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Widths(Widths &&other) :
      Component{
         other,
         content.width
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Widths(const Node &node) :
      Component{
         BodyText{},
         content.width
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Widths(
      const std::vector<resonances::Width> &width
   ) :
      Component{
         BodyText{},
         content.width
      },
      content{
         width
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Widths &operator=(const Widths &) = default;

   // move
   Widths &operator=(Widths &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/resonances/Widths/src/custom.hpp"

}; // class Widths

} // namespace resonances
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
