
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_RESONANCES_CHANNELS
#define NJOY_GNDSTK_V2_0_RESONANCES_CHANNELS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/resonances/Channel.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// resonances::
// class Channels
// -----------------------------------------------------------------------------

namespace resonances {

class Channels : public Component<Channels> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "resonances"; }
   static auto className() { return "Channels"; }
   static auto GNDSName() { return "channels"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         resonances::Channel{}
            / ++Child<>("channel")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<resonances::Channel> channel;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // channel
   const std::vector<resonances::Channel> &channel() const
      { return content.channel; }
   std::vector<resonances::Channel> &channel()
      { return content.channel; }

   // channel(index)
   const resonances::Channel &channel(const std::size_t index) const
      { return getter(channel(), index, "channel"); }
   resonances::Channel &channel(const std::size_t index)
      { return getter(channel(), index, "channel"); }

   // channel(label)
   const resonances::Channel &channel(const std::string &label) const
      { return getter(channel(), label, "channel"); }
   resonances::Channel &channel(const std::string &label)
      { return getter(channel(), label, "channel"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // channel(value)
   Channels &channel(const std::vector<resonances::Channel> &obj)
      { channel() = obj; return *this; }

   // channel(index,value)
   Channels &channel(
      const std::size_t index,
      const resonances::Channel &obj
   ) {
      channel(index) = obj; return *this;
   }

   // channel(label,value)
   Channels &channel(
      const std::string &label,
      const resonances::Channel &obj
   ) {
      channel(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Channels() :
      Component{
         BodyText{},
         content.channel
      }
   {
      Component::finish();
   }

   // copy
   Channels(const Channels &other) :
      Component{
         other,
         content.channel
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Channels(Channels &&other) :
      Component{
         other,
         content.channel
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Channels(const Node &node) :
      Component{
         BodyText{},
         content.channel
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Channels(
      const std::vector<resonances::Channel> &channel
   ) :
      Component{
         BodyText{},
         content.channel
      },
      content{
         channel
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Channels &operator=(const Channels &) = default;

   // move
   Channels &operator=(Channels &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/resonances/Channels/src/custom.hpp"

}; // class Channels

} // namespace resonances
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
