
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_RESONANCES_SPINGROUPS
#define NJOY_GNDSTK_V2_0_RESONANCES_SPINGROUPS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/resonances/SpinGroup.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// resonances::
// class SpinGroups
// -----------------------------------------------------------------------------

namespace resonances {

class SpinGroups : public Component<SpinGroups> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "resonances"; }
   static auto className() { return "SpinGroups"; }
   static auto GNDSName() { return "spinGroups"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         resonances::SpinGroup{}
            / ++Child<>("spinGroup")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<resonances::SpinGroup> spinGroup;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // spinGroup
   const std::vector<resonances::SpinGroup> &spinGroup() const
      { return content.spinGroup; }
   std::vector<resonances::SpinGroup> &spinGroup()
      { return content.spinGroup; }

   // spinGroup(index)
   const resonances::SpinGroup &spinGroup(const std::size_t index) const
      { return getter(spinGroup(), index, "spinGroup"); }
   resonances::SpinGroup &spinGroup(const std::size_t index)
      { return getter(spinGroup(), index, "spinGroup"); }

   // spinGroup(label)
   const resonances::SpinGroup &spinGroup(const std::string &label) const
      { return getter(spinGroup(), label, "spinGroup"); }
   resonances::SpinGroup &spinGroup(const std::string &label)
      { return getter(spinGroup(), label, "spinGroup"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // spinGroup(value)
   SpinGroups &spinGroup(const std::vector<resonances::SpinGroup> &obj)
      { spinGroup() = obj; return *this; }

   // spinGroup(index,value)
   SpinGroups &spinGroup(
      const std::size_t index,
      const resonances::SpinGroup &obj
   ) {
      spinGroup(index) = obj; return *this;
   }

   // spinGroup(label,value)
   SpinGroups &spinGroup(
      const std::string &label,
      const resonances::SpinGroup &obj
   ) {
      spinGroup(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   SpinGroups() :
      Component{
         BodyText{},
         content.spinGroup
      }
   {
      Component::finish();
   }

   // copy
   SpinGroups(const SpinGroups &other) :
      Component{
         other,
         content.spinGroup
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   SpinGroups(SpinGroups &&other) :
      Component{
         other,
         content.spinGroup
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   SpinGroups(const Node &node) :
      Component{
         BodyText{},
         content.spinGroup
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit SpinGroups(
      const std::vector<resonances::SpinGroup> &spinGroup
   ) :
      Component{
         BodyText{},
         content.spinGroup
      },
      content{
         spinGroup
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   SpinGroups &operator=(const SpinGroups &) = default;

   // move
   SpinGroups &operator=(SpinGroups &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/resonances/SpinGroups/src/custom.hpp"

}; // class SpinGroups

} // namespace resonances
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
