
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_RESONANCES_EXTERNALRMATRIX
#define NJOY_GNDSTK_V2_0_RESONANCES_EXTERNALRMATRIX

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Double.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// resonances::
// class ExternalRMatrix
// -----------------------------------------------------------------------------

namespace resonances {

class ExternalRMatrix : public Component<ExternalRMatrix> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "resonances"; }
   static auto className() { return "ExternalRMatrix"; }
   static auto GNDSName() { return "externalRMatrix"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{"Froehner"}
            / Meta<>("type") |
         // children
         containers::Double{}
            / ++Child<>("Double")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName type{"Froehner"};
      // children
      std::vector<containers::Double> Double;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // type
   const XMLName &type() const
      { return content.type; }
   XMLName &type()
      { return content.type; }

   // Double
   const std::vector<containers::Double> &Double() const
      { return content.Double; }
   std::vector<containers::Double> &Double()
      { return content.Double; }

   // Double(index)
   const containers::Double &Double(const std::size_t index) const
      { return getter(Double(), index, "Double"); }
   containers::Double &Double(const std::size_t index)
      { return getter(Double(), index, "Double"); }

   // Double(label)
   const containers::Double &Double(const std::string &label) const
      { return getter(Double(), label, "Double"); }
   containers::Double &Double(const std::string &label)
      { return getter(Double(), label, "Double"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // type(value)
   ExternalRMatrix &type(const XMLName &obj)
      { type() = obj; return *this; }

   // Double(value)
   ExternalRMatrix &Double(const std::vector<containers::Double> &obj)
      { Double() = obj; return *this; }

   // Double(index,value)
   ExternalRMatrix &Double(
      const std::size_t index,
      const containers::Double &obj
   ) {
      Double(index) = obj; return *this;
   }

   // Double(label,value)
   ExternalRMatrix &Double(
      const std::string &label,
      const containers::Double &obj
   ) {
      Double(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   ExternalRMatrix() :
      Component{
         BodyText{},
         content.type,
         content.Double
      }
   {
      Component::finish();
   }

   // copy
   ExternalRMatrix(const ExternalRMatrix &other) :
      Component{
         other,
         content.type,
         content.Double
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   ExternalRMatrix(ExternalRMatrix &&other) :
      Component{
         other,
         content.type,
         content.Double
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   ExternalRMatrix(const Node &node) :
      Component{
         BodyText{},
         content.type,
         content.Double
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit ExternalRMatrix(
      const XMLName &type,
      const std::vector<containers::Double> &Double
   ) :
      Component{
         BodyText{},
         content.type,
         content.Double
      },
      content{
         type,
         Double
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   ExternalRMatrix &operator=(const ExternalRMatrix &) = default;

   // move
   ExternalRMatrix &operator=(ExternalRMatrix &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/resonances/ExternalRMatrix/src/custom.hpp"

}; // class ExternalRMatrix

} // namespace resonances
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
