
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_RESONANCES_JS
#define NJOY_GNDSTK_V2_0_RESONANCES_JS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/resonances/J.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// resonances::
// class Js
// -----------------------------------------------------------------------------

namespace resonances {

class Js : public Component<Js> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "resonances"; }
   static auto className() { return "Js"; }
   static auto GNDSName() { return "Js"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         resonances::J{}
            / ++Child<>("J")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<resonances::J> J;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // J
   const std::vector<resonances::J> &J() const
      { return content.J; }
   std::vector<resonances::J> &J()
      { return content.J; }

   // J(index)
   const resonances::J &J(const std::size_t index) const
      { return getter(J(), index, "J"); }
   resonances::J &J(const std::size_t index)
      { return getter(J(), index, "J"); }

   // J(label)
   const resonances::J &J(const std::string &label) const
      { return getter(J(), label, "J"); }
   resonances::J &J(const std::string &label)
      { return getter(J(), label, "J"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // J(value)
   Js &J(const std::vector<resonances::J> &obj)
      { J() = obj; return *this; }

   // J(index,value)
   Js &J(
      const std::size_t index,
      const resonances::J &obj
   ) {
      J(index) = obj; return *this;
   }

   // J(label,value)
   Js &J(
      const std::string &label,
      const resonances::J &obj
   ) {
      J(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Js() :
      Component{
         BodyText{},
         content.J
      }
   {
      Component::finish();
   }

   // copy
   Js(const Js &other) :
      Component{
         other,
         content.J
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Js(Js &&other) :
      Component{
         other,
         content.J
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Js(const Node &node) :
      Component{
         BodyText{},
         content.J
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Js(
      const std::vector<resonances::J> &J
   ) :
      Component{
         BodyText{},
         content.J
      },
      content{
         J
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Js &operator=(const Js &) = default;

   // move
   Js &operator=(Js &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/resonances/Js/src/custom.hpp"

}; // class Js

} // namespace resonances
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
