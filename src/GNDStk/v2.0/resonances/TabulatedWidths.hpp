
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_RESONANCES_TABULATEDWIDTHS
#define NJOY_GNDSTK_V2_0_RESONANCES_TABULATEDWIDTHS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/resonances/Ls.hpp"
#include "GNDStk/v2.0/pops/PoPs.hpp"
#include "GNDStk/v2.0/resonances/HardSphereRadius.hpp"
#include "GNDStk/v2.0/resonances/ResonanceReactions.hpp"
#include "GNDStk/v2.0/resonances/ScatteringRadius.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// resonances::
// class TabulatedWidths
// -----------------------------------------------------------------------------

namespace resonances {

class TabulatedWidths : public Component<TabulatedWidths> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "resonances"; }
   static auto className() { return "TabulatedWidths"; }
   static auto GNDSName() { return "tabulatedWidths"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("approximation") |
         XMLName{}
            / Meta<>("label") |
         Defaulted<bool>{false}
            / Meta<>("useForSelfShieldingOnly") |
         // children
         resonances::Ls{}
            / --Child<>("Ls") |
         std::optional<pops::PoPs>{}
            / --Child<>("PoPs") |
         std::optional<resonances::HardSphereRadius>{}
            / --Child<>("hardSphereRadius") |
         resonances::ResonanceReactions{}
            / --Child<>("resonanceReactions") |
         std::optional<resonances::ScatteringRadius>{}
            / --Child<>("scatteringRadius")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
      static inline const bool useForSelfShieldingOnly = false;
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName approximation;
      XMLName label;
      Defaulted<bool> useForSelfShieldingOnly{false};
      // children
      resonances::Ls Ls;
      std::optional<pops::PoPs> PoPs;
      std::optional<resonances::HardSphereRadius> hardSphereRadius;
      resonances::ResonanceReactions resonanceReactions;
      std::optional<resonances::ScatteringRadius> scatteringRadius;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // approximation
   const XMLName &approximation() const
      { return content.approximation; }
   XMLName &approximation()
      { return content.approximation; }

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // useForSelfShieldingOnly
   const Defaulted<bool> &useForSelfShieldingOnly() const
      { return content.useForSelfShieldingOnly; }
   Defaulted<bool> &useForSelfShieldingOnly()
      { return content.useForSelfShieldingOnly; }

   // Ls
   const resonances::Ls &Ls() const
      { return content.Ls; }
   resonances::Ls &Ls()
      { return content.Ls; }

   // PoPs
   const std::optional<pops::PoPs> &PoPs() const
      { return content.PoPs; }
   std::optional<pops::PoPs> &PoPs()
      { return content.PoPs; }

   // hardSphereRadius
   const std::optional<resonances::HardSphereRadius> &hardSphereRadius() const
      { return content.hardSphereRadius; }
   std::optional<resonances::HardSphereRadius> &hardSphereRadius()
      { return content.hardSphereRadius; }

   // resonanceReactions
   const resonances::ResonanceReactions &resonanceReactions() const
      { return content.resonanceReactions; }
   resonances::ResonanceReactions &resonanceReactions()
      { return content.resonanceReactions; }

   // scatteringRadius
   const std::optional<resonances::ScatteringRadius> &scatteringRadius() const
      { return content.scatteringRadius; }
   std::optional<resonances::ScatteringRadius> &scatteringRadius()
      { return content.scatteringRadius; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // approximation(value)
   TabulatedWidths &approximation(const XMLName &obj)
      { approximation() = obj; return *this; }

   // label(value)
   TabulatedWidths &label(const XMLName &obj)
      { label() = obj; return *this; }

   // useForSelfShieldingOnly(value)
   TabulatedWidths &useForSelfShieldingOnly(const Defaulted<bool> &obj)
      { content.useForSelfShieldingOnly = obj; return *this; }
   TabulatedWidths &useForSelfShieldingOnly(const std::optional<bool> &obj)
      { content.useForSelfShieldingOnly = obj; return *this; }

   // Ls(value)
   TabulatedWidths &Ls(const resonances::Ls &obj)
      { Ls() = obj; return *this; }

   // PoPs(value)
   TabulatedWidths &PoPs(const std::optional<pops::PoPs> &obj)
      { PoPs() = obj; return *this; }

   // hardSphereRadius(value)
   TabulatedWidths &hardSphereRadius(const std::optional<resonances::HardSphereRadius> &obj)
      { hardSphereRadius() = obj; return *this; }

   // resonanceReactions(value)
   TabulatedWidths &resonanceReactions(const resonances::ResonanceReactions &obj)
      { resonanceReactions() = obj; return *this; }

   // scatteringRadius(value)
   TabulatedWidths &scatteringRadius(const std::optional<resonances::ScatteringRadius> &obj)
      { scatteringRadius() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   TabulatedWidths() :
      Component{
         BodyText{},
         content.approximation,
         content.label,
         content.useForSelfShieldingOnly,
         content.Ls,
         content.PoPs,
         content.hardSphereRadius,
         content.resonanceReactions,
         content.scatteringRadius
      }
   {
      Component::finish();
   }

   // copy
   TabulatedWidths(const TabulatedWidths &other) :
      Component{
         other,
         content.approximation,
         content.label,
         content.useForSelfShieldingOnly,
         content.Ls,
         content.PoPs,
         content.hardSphereRadius,
         content.resonanceReactions,
         content.scatteringRadius
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   TabulatedWidths(TabulatedWidths &&other) :
      Component{
         other,
         content.approximation,
         content.label,
         content.useForSelfShieldingOnly,
         content.Ls,
         content.PoPs,
         content.hardSphereRadius,
         content.resonanceReactions,
         content.scatteringRadius
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   TabulatedWidths(const Node &node) :
      Component{
         BodyText{},
         content.approximation,
         content.label,
         content.useForSelfShieldingOnly,
         content.Ls,
         content.PoPs,
         content.hardSphereRadius,
         content.resonanceReactions,
         content.scatteringRadius
      }
   {
      Component::finish(node);
   }

   // from fields
   // std::optional replaces Defaulted; this class knows the default(s)
   explicit TabulatedWidths(
      const XMLName &approximation,
      const XMLName &label,
      const std::optional<bool> &useForSelfShieldingOnly,
      const resonances::Ls &Ls,
      const std::optional<pops::PoPs> &PoPs,
      const std::optional<resonances::HardSphereRadius> &hardSphereRadius,
      const resonances::ResonanceReactions &resonanceReactions,
      const std::optional<resonances::ScatteringRadius> &scatteringRadius
   ) :
      Component{
         BodyText{},
         content.approximation,
         content.label,
         content.useForSelfShieldingOnly,
         content.Ls,
         content.PoPs,
         content.hardSphereRadius,
         content.resonanceReactions,
         content.scatteringRadius
      },
      content{
         approximation,
         label,
         Defaulted<bool>(defaults.useForSelfShieldingOnly,useForSelfShieldingOnly),
         Ls,
         PoPs,
         hardSphereRadius,
         resonanceReactions,
         scatteringRadius
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   TabulatedWidths &operator=(const TabulatedWidths &) = default;

   // move
   TabulatedWidths &operator=(TabulatedWidths &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/resonances/TabulatedWidths/src/custom.hpp"

}; // class TabulatedWidths

} // namespace resonances
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
