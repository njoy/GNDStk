
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_DECAYDATA
#define NJOY_GNDSTK_V2_0_POPS_DECAYDATA

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/AverageEnergies.hpp"
#include "GNDStk/v2.0/pops/DecayModes.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class DecayData
// -----------------------------------------------------------------------------

namespace pops {

class DecayData : public Component<DecayData> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "DecayData"; }
   static auto GNDSName() { return "decayData"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         std::optional<pops::AverageEnergies>{}
            / --Child<>("averageEnergies") |
         std::optional<pops::DecayModes>{}
            / --Child<>("decayModes")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::optional<pops::AverageEnergies> averageEnergies;
      std::optional<pops::DecayModes> decayModes;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // averageEnergies
   const std::optional<pops::AverageEnergies> &averageEnergies() const
      { return content.averageEnergies; }
   std::optional<pops::AverageEnergies> &averageEnergies()
      { return content.averageEnergies; }

   // decayModes
   const std::optional<pops::DecayModes> &decayModes() const
      { return content.decayModes; }
   std::optional<pops::DecayModes> &decayModes()
      { return content.decayModes; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // averageEnergies(value)
   DecayData &averageEnergies(const std::optional<pops::AverageEnergies> &obj)
      { averageEnergies() = obj; return *this; }

   // decayModes(value)
   DecayData &decayModes(const std::optional<pops::DecayModes> &obj)
      { decayModes() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   DecayData() :
      Component{
         BodyText{},
         content.averageEnergies,
         content.decayModes
      }
   {
      Component::finish();
   }

   // copy
   DecayData(const DecayData &other) :
      Component{
         other,
         content.averageEnergies,
         content.decayModes
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   DecayData(DecayData &&other) :
      Component{
         other,
         content.averageEnergies,
         content.decayModes
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   DecayData(const Node &node) :
      Component{
         BodyText{},
         content.averageEnergies,
         content.decayModes
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit DecayData(
      const std::optional<pops::AverageEnergies> &averageEnergies,
      const std::optional<pops::DecayModes> &decayModes
   ) :
      Component{
         BodyText{},
         content.averageEnergies,
         content.decayModes
      },
      content{
         averageEnergies,
         decayModes
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   DecayData &operator=(const DecayData &) = default;

   // move
   DecayData &operator=(DecayData &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/DecayData/src/custom.hpp"

}; // class DecayData

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
