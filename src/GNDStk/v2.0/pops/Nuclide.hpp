
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_NUCLIDE
#define NJOY_GNDSTK_V2_0_POPS_NUCLIDE

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/Charge.hpp"
#include "GNDStk/v2.0/pops/DecayData.hpp"
#include "GNDStk/v2.0/fissionFragmentData/FissionFragmentData.hpp"
#include "GNDStk/v2.0/pops/Mass.hpp"
#include "GNDStk/v2.0/pops/Nucleus.hpp"
#include "GNDStk/v2.0/pops/Parity.hpp"
#include "GNDStk/v2.0/pops/Spin.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Nuclide
// -----------------------------------------------------------------------------

namespace pops {

class Nuclide : public Component<Nuclide> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Nuclide"; }
   static auto GNDSName() { return "nuclide"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("id") |
         // children
         std::optional<pops::Charge>{}
            / --Child<>("charge") |
         std::optional<pops::DecayData>{}
            / --Child<>("decayData") |
         std::optional<fissionFragmentData::FissionFragmentData>{}
            / --Child<>("fissionFragmentData") |
         std::optional<pops::Mass>{}
            / --Child<>("mass") |
         std::optional<pops::Nucleus>{}
            / --Child<>("nucleus") |
         std::optional<pops::Parity>{}
            / --Child<>("parity") |
         std::optional<pops::Spin>{}
            / --Child<>("spin")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName id;
      // children
      std::optional<pops::Charge> charge;
      std::optional<pops::DecayData> decayData;
      std::optional<fissionFragmentData::FissionFragmentData> fissionFragmentData;
      std::optional<pops::Mass> mass;
      std::optional<pops::Nucleus> nucleus;
      std::optional<pops::Parity> parity;
      std::optional<pops::Spin> spin;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // id
   const XMLName &id() const
      { return content.id; }
   XMLName &id()
      { return content.id; }

   // charge
   const std::optional<pops::Charge> &charge() const
      { return content.charge; }
   std::optional<pops::Charge> &charge()
      { return content.charge; }

   // decayData
   const std::optional<pops::DecayData> &decayData() const
      { return content.decayData; }
   std::optional<pops::DecayData> &decayData()
      { return content.decayData; }

   // fissionFragmentData
   const std::optional<fissionFragmentData::FissionFragmentData> &fissionFragmentData() const
      { return content.fissionFragmentData; }
   std::optional<fissionFragmentData::FissionFragmentData> &fissionFragmentData()
      { return content.fissionFragmentData; }

   // mass
   const std::optional<pops::Mass> &mass() const
      { return content.mass; }
   std::optional<pops::Mass> &mass()
      { return content.mass; }

   // nucleus
   const std::optional<pops::Nucleus> &nucleus() const
      { return content.nucleus; }
   std::optional<pops::Nucleus> &nucleus()
      { return content.nucleus; }

   // parity
   const std::optional<pops::Parity> &parity() const
      { return content.parity; }
   std::optional<pops::Parity> &parity()
      { return content.parity; }

   // spin
   const std::optional<pops::Spin> &spin() const
      { return content.spin; }
   std::optional<pops::Spin> &spin()
      { return content.spin; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // id(value)
   Nuclide &id(const XMLName &obj)
      { id() = obj; return *this; }

   // charge(value)
   Nuclide &charge(const std::optional<pops::Charge> &obj)
      { charge() = obj; return *this; }

   // decayData(value)
   Nuclide &decayData(const std::optional<pops::DecayData> &obj)
      { decayData() = obj; return *this; }

   // fissionFragmentData(value)
   Nuclide &fissionFragmentData(const std::optional<fissionFragmentData::FissionFragmentData> &obj)
      { fissionFragmentData() = obj; return *this; }

   // mass(value)
   Nuclide &mass(const std::optional<pops::Mass> &obj)
      { mass() = obj; return *this; }

   // nucleus(value)
   Nuclide &nucleus(const std::optional<pops::Nucleus> &obj)
      { nucleus() = obj; return *this; }

   // parity(value)
   Nuclide &parity(const std::optional<pops::Parity> &obj)
      { parity() = obj; return *this; }

   // spin(value)
   Nuclide &spin(const std::optional<pops::Spin> &obj)
      { spin() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Nuclide() :
      Component{
         BodyText{},
         content.id,
         content.charge,
         content.decayData,
         content.fissionFragmentData,
         content.mass,
         content.nucleus,
         content.parity,
         content.spin
      }
   {
      Component::finish();
   }

   // copy
   Nuclide(const Nuclide &other) :
      Component{
         other,
         content.id,
         content.charge,
         content.decayData,
         content.fissionFragmentData,
         content.mass,
         content.nucleus,
         content.parity,
         content.spin
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Nuclide(Nuclide &&other) :
      Component{
         other,
         content.id,
         content.charge,
         content.decayData,
         content.fissionFragmentData,
         content.mass,
         content.nucleus,
         content.parity,
         content.spin
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Nuclide(const Node &node) :
      Component{
         BodyText{},
         content.id,
         content.charge,
         content.decayData,
         content.fissionFragmentData,
         content.mass,
         content.nucleus,
         content.parity,
         content.spin
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Nuclide(
      const XMLName &id,
      const std::optional<pops::Charge> &charge,
      const std::optional<pops::DecayData> &decayData,
      const std::optional<fissionFragmentData::FissionFragmentData> &fissionFragmentData,
      const std::optional<pops::Mass> &mass,
      const std::optional<pops::Nucleus> &nucleus,
      const std::optional<pops::Parity> &parity,
      const std::optional<pops::Spin> &spin
   ) :
      Component{
         BodyText{},
         content.id,
         content.charge,
         content.decayData,
         content.fissionFragmentData,
         content.mass,
         content.nucleus,
         content.parity,
         content.spin
      },
      content{
         id,
         charge,
         decayData,
         fissionFragmentData,
         mass,
         nucleus,
         parity,
         spin
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Nuclide &operator=(const Nuclide &) = default;

   // move
   Nuclide &operator=(Nuclide &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Nuclide/src/custom.hpp"

}; // class Nuclide

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
