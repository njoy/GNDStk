
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_ALIAS
#define NJOY_GNDSTK_V2_0_POPS_ALIAS

// core interface
#include "GNDStk.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Alias
// -----------------------------------------------------------------------------

namespace pops {

class Alias : public Component<Alias> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Alias"; }
   static auto GNDSName() { return "alias"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("id") |
         XMLName{}
            / Meta<>("pid")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName id;
      XMLName pid;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // id
   const XMLName &id() const
      { return content.id; }
   XMLName &id()
      { return content.id; }

   // pid
   const XMLName &pid() const
      { return content.pid; }
   XMLName &pid()
      { return content.pid; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // id(value)
   Alias &id(const XMLName &obj)
      { id() = obj; return *this; }

   // pid(value)
   Alias &pid(const XMLName &obj)
      { pid() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Alias() :
      Component{
         BodyText{},
         content.id,
         content.pid
      }
   {
      Component::finish();
   }

   // copy
   Alias(const Alias &other) :
      Component{
         other,
         content.id,
         content.pid
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Alias(Alias &&other) :
      Component{
         other,
         content.id,
         content.pid
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Alias(const Node &node) :
      Component{
         BodyText{},
         content.id,
         content.pid
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Alias(
      const XMLName &id,
      const XMLName &pid
   ) :
      Component{
         BodyText{},
         content.id,
         content.pid
      },
      content{
         id,
         pid
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Alias &operator=(const Alias &) = default;

   // move
   Alias &operator=(Alias &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Alias/src/custom.hpp"

}; // class Alias

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
