
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_AVERAGEENERGIES
#define NJOY_GNDSTK_V2_0_POPS_AVERAGEENERGIES

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/AverageEnergy.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class AverageEnergies
// -----------------------------------------------------------------------------

namespace pops {

class AverageEnergies : public Component<AverageEnergies> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "AverageEnergies"; }
   static auto GNDSName() { return "averageEnergies"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         pops::AverageEnergy{}
            / ++Child<>("averageEnergy")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<pops::AverageEnergy> averageEnergy;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // averageEnergy
   const std::vector<pops::AverageEnergy> &averageEnergy() const
      { return content.averageEnergy; }
   std::vector<pops::AverageEnergy> &averageEnergy()
      { return content.averageEnergy; }

   // averageEnergy(index)
   const pops::AverageEnergy &averageEnergy(const std::size_t index) const
      { return getter(averageEnergy(), index, "averageEnergy"); }
   pops::AverageEnergy &averageEnergy(const std::size_t index)
      { return getter(averageEnergy(), index, "averageEnergy"); }

   // averageEnergy(label)
   const pops::AverageEnergy &averageEnergy(const std::string &label) const
      { return getter(averageEnergy(), label, "averageEnergy"); }
   pops::AverageEnergy &averageEnergy(const std::string &label)
      { return getter(averageEnergy(), label, "averageEnergy"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // averageEnergy(value)
   AverageEnergies &averageEnergy(const std::vector<pops::AverageEnergy> &obj)
      { averageEnergy() = obj; return *this; }

   // averageEnergy(index,value)
   AverageEnergies &averageEnergy(
      const std::size_t index,
      const pops::AverageEnergy &obj
   ) {
      averageEnergy(index) = obj; return *this;
   }

   // averageEnergy(label,value)
   AverageEnergies &averageEnergy(
      const std::string &label,
      const pops::AverageEnergy &obj
   ) {
      averageEnergy(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   AverageEnergies() :
      Component{
         BodyText{},
         content.averageEnergy
      }
   {
      Component::finish();
   }

   // copy
   AverageEnergies(const AverageEnergies &other) :
      Component{
         other,
         content.averageEnergy
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   AverageEnergies(AverageEnergies &&other) :
      Component{
         other,
         content.averageEnergy
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   AverageEnergies(const Node &node) :
      Component{
         BodyText{},
         content.averageEnergy
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit AverageEnergies(
      const std::vector<pops::AverageEnergy> &averageEnergy
   ) :
      Component{
         BodyText{},
         content.averageEnergy
      },
      content{
         averageEnergy
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   AverageEnergies &operator=(const AverageEnergies &) = default;

   // move
   AverageEnergies &operator=(AverageEnergies &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/AverageEnergies/src/custom.hpp"

}; // class AverageEnergies

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
