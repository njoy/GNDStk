
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_ENERGY
#define NJOY_GNDSTK_V2_0_POPS_ENERGY

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Double.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Energy
// -----------------------------------------------------------------------------

namespace pops {

class Energy : public Component<Energy> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Energy"; }
   static auto GNDSName() { return "energy"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("unit") |
         Float64{}
            / Meta<>("value") |
         // children
         std::optional<containers::Double>{}
            / ++Child<>("Double")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName unit;
      Float64 value;
      // children
      std::optional<std::vector<containers::Double>> Double;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // unit
   const XMLName &unit() const
      { return content.unit; }
   XMLName &unit()
      { return content.unit; }

   // value
   const Float64 &value() const
      { return content.value; }
   Float64 &value()
      { return content.value; }

   // Double
   const std::optional<std::vector<containers::Double>> &Double() const
      { return content.Double; }
   std::optional<std::vector<containers::Double>> &Double()
      { return content.Double; }

   // Double(index)
   const containers::Double &Double(const std::size_t index) const
      { return getter(Double(), index, "Double"); }
   containers::Double &Double(const std::size_t index)
      { return getter(Double(), index, "Double"); }

   // Double(label)
   const containers::Double &Double(const std::string &label) const
      { return getter(Double(), label, "Double"); }
   containers::Double &Double(const std::string &label)
      { return getter(Double(), label, "Double"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // unit(value)
   Energy &unit(const XMLName &obj)
      { unit() = obj; return *this; }

   // value(value)
   Energy &value(const Float64 &obj)
      { value() = obj; return *this; }

   // Double(value)
   Energy &Double(const std::optional<std::vector<containers::Double>> &obj)
      { Double() = obj; return *this; }

   // Double(index,value)
   Energy &Double(
      const std::size_t index,
      const containers::Double &obj
   ) {
      Double(index) = obj; return *this;
   }

   // Double(label,value)
   Energy &Double(
      const std::string &label,
      const containers::Double &obj
   ) {
      Double(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Energy() :
      Component{
         BodyText{},
         content.unit,
         content.value,
         content.Double
      }
   {
      Component::finish();
   }

   // copy
   Energy(const Energy &other) :
      Component{
         other,
         content.unit,
         content.value,
         content.Double
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Energy(Energy &&other) :
      Component{
         other,
         content.unit,
         content.value,
         content.Double
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Energy(const Node &node) :
      Component{
         BodyText{},
         content.unit,
         content.value,
         content.Double
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Energy(
      const XMLName &unit,
      const Float64 &value,
      const std::optional<std::vector<containers::Double>> &Double
   ) :
      Component{
         BodyText{},
         content.unit,
         content.value,
         content.Double
      },
      content{
         unit,
         value,
         Double
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Energy &operator=(const Energy &) = default;

   // move
   Energy &operator=(Energy &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Energy/src/custom.hpp"

}; // class Energy

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
