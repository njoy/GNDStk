
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_LEPTON
#define NJOY_GNDSTK_V2_0_POPS_LEPTON

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/Charge.hpp"
#include "GNDStk/v2.0/pops/DecayData.hpp"
#include "GNDStk/v2.0/pops/Halflife.hpp"
#include "GNDStk/v2.0/pops/Mass.hpp"
#include "GNDStk/v2.0/pops/Parity.hpp"
#include "GNDStk/v2.0/pops/Spin.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Lepton
// -----------------------------------------------------------------------------

namespace pops {

class Lepton : public Component<Lepton> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Lepton"; }
   static auto GNDSName() { return "lepton"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<XMLName>{}
            / Meta<>("generation") |
         XMLName{}
            / Meta<>("id") |
         // children
         std::optional<pops::Charge>{}
            / --Child<>("charge") |
         std::optional<pops::DecayData>{}
            / --Child<>("decayData") |
         std::optional<pops::Halflife>{}
            / --Child<>("halflife") |
         std::optional<pops::Mass>{}
            / --Child<>("mass") |
         std::optional<pops::Parity>{}
            / --Child<>("parity") |
         std::optional<pops::Spin>{}
            / --Child<>("spin")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<XMLName> generation;
      XMLName id;
      // children
      std::optional<pops::Charge> charge;
      std::optional<pops::DecayData> decayData;
      std::optional<pops::Halflife> halflife;
      std::optional<pops::Mass> mass;
      std::optional<pops::Parity> parity;
      std::optional<pops::Spin> spin;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // generation
   const std::optional<XMLName> &generation() const
      { return content.generation; }
   std::optional<XMLName> &generation()
      { return content.generation; }

   // id
   const XMLName &id() const
      { return content.id; }
   XMLName &id()
      { return content.id; }

   // charge
   const std::optional<pops::Charge> &charge() const
      { return content.charge; }
   std::optional<pops::Charge> &charge()
      { return content.charge; }

   // decayData
   const std::optional<pops::DecayData> &decayData() const
      { return content.decayData; }
   std::optional<pops::DecayData> &decayData()
      { return content.decayData; }

   // halflife
   const std::optional<pops::Halflife> &halflife() const
      { return content.halflife; }
   std::optional<pops::Halflife> &halflife()
      { return content.halflife; }

   // mass
   const std::optional<pops::Mass> &mass() const
      { return content.mass; }
   std::optional<pops::Mass> &mass()
      { return content.mass; }

   // parity
   const std::optional<pops::Parity> &parity() const
      { return content.parity; }
   std::optional<pops::Parity> &parity()
      { return content.parity; }

   // spin
   const std::optional<pops::Spin> &spin() const
      { return content.spin; }
   std::optional<pops::Spin> &spin()
      { return content.spin; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // generation(value)
   Lepton &generation(const std::optional<XMLName> &obj)
      { generation() = obj; return *this; }

   // id(value)
   Lepton &id(const XMLName &obj)
      { id() = obj; return *this; }

   // charge(value)
   Lepton &charge(const std::optional<pops::Charge> &obj)
      { charge() = obj; return *this; }

   // decayData(value)
   Lepton &decayData(const std::optional<pops::DecayData> &obj)
      { decayData() = obj; return *this; }

   // halflife(value)
   Lepton &halflife(const std::optional<pops::Halflife> &obj)
      { halflife() = obj; return *this; }

   // mass(value)
   Lepton &mass(const std::optional<pops::Mass> &obj)
      { mass() = obj; return *this; }

   // parity(value)
   Lepton &parity(const std::optional<pops::Parity> &obj)
      { parity() = obj; return *this; }

   // spin(value)
   Lepton &spin(const std::optional<pops::Spin> &obj)
      { spin() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Lepton() :
      Component{
         BodyText{},
         content.generation,
         content.id,
         content.charge,
         content.decayData,
         content.halflife,
         content.mass,
         content.parity,
         content.spin
      }
   {
      Component::finish();
   }

   // copy
   Lepton(const Lepton &other) :
      Component{
         other,
         content.generation,
         content.id,
         content.charge,
         content.decayData,
         content.halflife,
         content.mass,
         content.parity,
         content.spin
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Lepton(Lepton &&other) :
      Component{
         other,
         content.generation,
         content.id,
         content.charge,
         content.decayData,
         content.halflife,
         content.mass,
         content.parity,
         content.spin
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Lepton(const Node &node) :
      Component{
         BodyText{},
         content.generation,
         content.id,
         content.charge,
         content.decayData,
         content.halflife,
         content.mass,
         content.parity,
         content.spin
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Lepton(
      const std::optional<XMLName> &generation,
      const XMLName &id,
      const std::optional<pops::Charge> &charge,
      const std::optional<pops::DecayData> &decayData,
      const std::optional<pops::Halflife> &halflife,
      const std::optional<pops::Mass> &mass,
      const std::optional<pops::Parity> &parity,
      const std::optional<pops::Spin> &spin
   ) :
      Component{
         BodyText{},
         content.generation,
         content.id,
         content.charge,
         content.decayData,
         content.halflife,
         content.mass,
         content.parity,
         content.spin
      },
      content{
         generation,
         id,
         charge,
         decayData,
         halflife,
         mass,
         parity,
         spin
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Lepton &operator=(const Lepton &) = default;

   // move
   Lepton &operator=(Lepton &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Lepton/src/custom.hpp"

}; // class Lepton

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
