
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_CONFIGURATIONS
#define NJOY_GNDSTK_V2_0_POPS_CONFIGURATIONS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/Configuration.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Configurations
// -----------------------------------------------------------------------------

namespace pops {

class Configurations : public Component<Configurations> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Configurations"; }
   static auto GNDSName() { return "configurations"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         pops::Configuration{}
            / ++Child<>("configuration")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<pops::Configuration> configuration;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // configuration
   const std::vector<pops::Configuration> &configuration() const
      { return content.configuration; }
   std::vector<pops::Configuration> &configuration()
      { return content.configuration; }

   // configuration(index)
   const pops::Configuration &configuration(const std::size_t index) const
      { return getter(configuration(), index, "configuration"); }
   pops::Configuration &configuration(const std::size_t index)
      { return getter(configuration(), index, "configuration"); }

   // configuration(label)
   const pops::Configuration &configuration(const std::string &label) const
      { return getter(configuration(), label, "configuration"); }
   pops::Configuration &configuration(const std::string &label)
      { return getter(configuration(), label, "configuration"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // configuration(value)
   Configurations &configuration(const std::vector<pops::Configuration> &obj)
      { configuration() = obj; return *this; }

   // configuration(index,value)
   Configurations &configuration(
      const std::size_t index,
      const pops::Configuration &obj
   ) {
      configuration(index) = obj; return *this;
   }

   // configuration(label,value)
   Configurations &configuration(
      const std::string &label,
      const pops::Configuration &obj
   ) {
      configuration(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Configurations() :
      Component{
         BodyText{},
         content.configuration
      }
   {
      Component::finish();
   }

   // copy
   Configurations(const Configurations &other) :
      Component{
         other,
         content.configuration
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Configurations(Configurations &&other) :
      Component{
         other,
         content.configuration
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Configurations(const Node &node) :
      Component{
         BodyText{},
         content.configuration
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Configurations(
      const std::vector<pops::Configuration> &configuration
   ) :
      Component{
         BodyText{},
         content.configuration
      },
      content{
         configuration
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Configurations &operator=(const Configurations &) = default;

   // move
   Configurations &operator=(Configurations &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Configurations/src/custom.hpp"

}; // class Configurations

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
