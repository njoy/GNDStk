
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_SPECTRA
#define NJOY_GNDSTK_V2_0_POPS_SPECTRA

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/Spectrum.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Spectra
// -----------------------------------------------------------------------------

namespace pops {

class Spectra : public Component<Spectra> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Spectra"; }
   static auto GNDSName() { return "spectra"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         pops::Spectrum{}
            / ++Child<>("spectrum")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<pops::Spectrum> spectrum;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // spectrum
   const std::vector<pops::Spectrum> &spectrum() const
      { return content.spectrum; }
   std::vector<pops::Spectrum> &spectrum()
      { return content.spectrum; }

   // spectrum(index)
   const pops::Spectrum &spectrum(const std::size_t index) const
      { return getter(spectrum(), index, "spectrum"); }
   pops::Spectrum &spectrum(const std::size_t index)
      { return getter(spectrum(), index, "spectrum"); }

   // spectrum(label)
   const pops::Spectrum &spectrum(const std::string &label) const
      { return getter(spectrum(), label, "spectrum"); }
   pops::Spectrum &spectrum(const std::string &label)
      { return getter(spectrum(), label, "spectrum"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // spectrum(value)
   Spectra &spectrum(const std::vector<pops::Spectrum> &obj)
      { spectrum() = obj; return *this; }

   // spectrum(index,value)
   Spectra &spectrum(
      const std::size_t index,
      const pops::Spectrum &obj
   ) {
      spectrum(index) = obj; return *this;
   }

   // spectrum(label,value)
   Spectra &spectrum(
      const std::string &label,
      const pops::Spectrum &obj
   ) {
      spectrum(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Spectra() :
      Component{
         BodyText{},
         content.spectrum
      }
   {
      Component::finish();
   }

   // copy
   Spectra(const Spectra &other) :
      Component{
         other,
         content.spectrum
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Spectra(Spectra &&other) :
      Component{
         other,
         content.spectrum
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Spectra(const Node &node) :
      Component{
         BodyText{},
         content.spectrum
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Spectra(
      const std::vector<pops::Spectrum> &spectrum
   ) :
      Component{
         BodyText{},
         content.spectrum
      },
      content{
         spectrum
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Spectra &operator=(const Spectra &) = default;

   // move
   Spectra &operator=(Spectra &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Spectra/src/custom.hpp"

}; // class Spectra

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
