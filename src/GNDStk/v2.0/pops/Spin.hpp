
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_SPIN
#define NJOY_GNDSTK_V2_0_POPS_SPIN

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Fraction.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Spin
// -----------------------------------------------------------------------------

namespace pops {

class Spin : public Component<Spin> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Spin"; }
   static auto GNDSName() { return "spin"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         std::optional<containers::Fraction>{}
            / ++Child<>("fraction")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::optional<std::vector<containers::Fraction>> fraction;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // fraction
   const std::optional<std::vector<containers::Fraction>> &fraction() const
      { return content.fraction; }
   std::optional<std::vector<containers::Fraction>> &fraction()
      { return content.fraction; }

   // fraction(index)
   const containers::Fraction &fraction(const std::size_t index) const
      { return getter(fraction(), index, "fraction"); }
   containers::Fraction &fraction(const std::size_t index)
      { return getter(fraction(), index, "fraction"); }

   // fraction(label)
   const containers::Fraction &fraction(const std::string &label) const
      { return getter(fraction(), label, "fraction"); }
   containers::Fraction &fraction(const std::string &label)
      { return getter(fraction(), label, "fraction"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // fraction(value)
   Spin &fraction(const std::optional<std::vector<containers::Fraction>> &obj)
      { fraction() = obj; return *this; }

   // fraction(index,value)
   Spin &fraction(
      const std::size_t index,
      const containers::Fraction &obj
   ) {
      fraction(index) = obj; return *this;
   }

   // fraction(label,value)
   Spin &fraction(
      const std::string &label,
      const containers::Fraction &obj
   ) {
      fraction(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Spin() :
      Component{
         BodyText{},
         content.fraction
      }
   {
      Component::finish();
   }

   // copy
   Spin(const Spin &other) :
      Component{
         other,
         content.fraction
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Spin(Spin &&other) :
      Component{
         other,
         content.fraction
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Spin(const Node &node) :
      Component{
         BodyText{},
         content.fraction
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Spin(
      const std::optional<std::vector<containers::Fraction>> &fraction
   ) :
      Component{
         BodyText{},
         content.fraction
      },
      content{
         fraction
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Spin &operator=(const Spin &) = default;

   // move
   Spin &operator=(Spin &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Spin/src/custom.hpp"

}; // class Spin

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
