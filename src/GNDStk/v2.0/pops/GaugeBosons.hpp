
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_GAUGEBOSONS
#define NJOY_GNDSTK_V2_0_POPS_GAUGEBOSONS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/GaugeBoson.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class GaugeBosons
// -----------------------------------------------------------------------------

namespace pops {

class GaugeBosons : public Component<GaugeBosons> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "GaugeBosons"; }
   static auto GNDSName() { return "gaugeBosons"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         std::optional<pops::GaugeBoson>{}
            / ++Child<>("gaugeBoson")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::optional<std::vector<pops::GaugeBoson>> gaugeBoson;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // gaugeBoson
   const std::optional<std::vector<pops::GaugeBoson>> &gaugeBoson() const
      { return content.gaugeBoson; }
   std::optional<std::vector<pops::GaugeBoson>> &gaugeBoson()
      { return content.gaugeBoson; }

   // gaugeBoson(index)
   const pops::GaugeBoson &gaugeBoson(const std::size_t index) const
      { return getter(gaugeBoson(), index, "gaugeBoson"); }
   pops::GaugeBoson &gaugeBoson(const std::size_t index)
      { return getter(gaugeBoson(), index, "gaugeBoson"); }

   // gaugeBoson(label)
   const pops::GaugeBoson &gaugeBoson(const std::string &label) const
      { return getter(gaugeBoson(), label, "gaugeBoson"); }
   pops::GaugeBoson &gaugeBoson(const std::string &label)
      { return getter(gaugeBoson(), label, "gaugeBoson"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // gaugeBoson(value)
   GaugeBosons &gaugeBoson(const std::optional<std::vector<pops::GaugeBoson>> &obj)
      { gaugeBoson() = obj; return *this; }

   // gaugeBoson(index,value)
   GaugeBosons &gaugeBoson(
      const std::size_t index,
      const pops::GaugeBoson &obj
   ) {
      gaugeBoson(index) = obj; return *this;
   }

   // gaugeBoson(label,value)
   GaugeBosons &gaugeBoson(
      const std::string &label,
      const pops::GaugeBoson &obj
   ) {
      gaugeBoson(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   GaugeBosons() :
      Component{
         BodyText{},
         content.gaugeBoson
      }
   {
      Component::finish();
   }

   // copy
   GaugeBosons(const GaugeBosons &other) :
      Component{
         other,
         content.gaugeBoson
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   GaugeBosons(GaugeBosons &&other) :
      Component{
         other,
         content.gaugeBoson
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   GaugeBosons(const Node &node) :
      Component{
         BodyText{},
         content.gaugeBoson
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit GaugeBosons(
      const std::optional<std::vector<pops::GaugeBoson>> &gaugeBoson
   ) :
      Component{
         BodyText{},
         content.gaugeBoson
      },
      content{
         gaugeBoson
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   GaugeBosons &operator=(const GaugeBosons &) = default;

   // move
   GaugeBosons &operator=(GaugeBosons &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/GaugeBosons/src/custom.hpp"

}; // class GaugeBosons

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
