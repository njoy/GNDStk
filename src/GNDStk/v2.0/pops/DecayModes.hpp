
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_DECAYMODES
#define NJOY_GNDSTK_V2_0_POPS_DECAYMODES

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/DecayMode.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class DecayModes
// -----------------------------------------------------------------------------

namespace pops {

class DecayModes : public Component<DecayModes> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "DecayModes"; }
   static auto GNDSName() { return "decayModes"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         pops::DecayMode{}
            / ++Child<>("decayMode")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<pops::DecayMode> decayMode;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // decayMode
   const std::vector<pops::DecayMode> &decayMode() const
      { return content.decayMode; }
   std::vector<pops::DecayMode> &decayMode()
      { return content.decayMode; }

   // decayMode(index)
   const pops::DecayMode &decayMode(const std::size_t index) const
      { return getter(decayMode(), index, "decayMode"); }
   pops::DecayMode &decayMode(const std::size_t index)
      { return getter(decayMode(), index, "decayMode"); }

   // decayMode(label)
   const pops::DecayMode &decayMode(const std::string &label) const
      { return getter(decayMode(), label, "decayMode"); }
   pops::DecayMode &decayMode(const std::string &label)
      { return getter(decayMode(), label, "decayMode"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // decayMode(value)
   DecayModes &decayMode(const std::vector<pops::DecayMode> &obj)
      { decayMode() = obj; return *this; }

   // decayMode(index,value)
   DecayModes &decayMode(
      const std::size_t index,
      const pops::DecayMode &obj
   ) {
      decayMode(index) = obj; return *this;
   }

   // decayMode(label,value)
   DecayModes &decayMode(
      const std::string &label,
      const pops::DecayMode &obj
   ) {
      decayMode(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   DecayModes() :
      Component{
         BodyText{},
         content.decayMode
      }
   {
      Component::finish();
   }

   // copy
   DecayModes(const DecayModes &other) :
      Component{
         other,
         content.decayMode
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   DecayModes(DecayModes &&other) :
      Component{
         other,
         content.decayMode
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   DecayModes(const Node &node) :
      Component{
         BodyText{},
         content.decayMode
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit DecayModes(
      const std::vector<pops::DecayMode> &decayMode
   ) :
      Component{
         BodyText{},
         content.decayMode
      },
      content{
         decayMode
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   DecayModes &operator=(const DecayModes &) = default;

   // move
   DecayModes &operator=(DecayModes &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/DecayModes/src/custom.hpp"

}; // class DecayModes

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
