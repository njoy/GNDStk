
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_Q
#define NJOY_GNDSTK_V2_0_POPS_Q

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Double.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Q
// -----------------------------------------------------------------------------

namespace pops {

class Q : public Component<Q> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Q"; }
   static auto GNDSName() { return "Q"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         containers::Double{}
            / ++Child<>("Double")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<containers::Double> Double;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // Double
   const std::vector<containers::Double> &Double() const
      { return content.Double; }
   std::vector<containers::Double> &Double()
      { return content.Double; }

   // Double(index)
   const containers::Double &Double(const std::size_t index) const
      { return getter(Double(), index, "Double"); }
   containers::Double &Double(const std::size_t index)
      { return getter(Double(), index, "Double"); }

   // Double(label)
   const containers::Double &Double(const std::string &label) const
      { return getter(Double(), label, "Double"); }
   containers::Double &Double(const std::string &label)
      { return getter(Double(), label, "Double"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // Double(value)
   Q &Double(const std::vector<containers::Double> &obj)
      { Double() = obj; return *this; }

   // Double(index,value)
   Q &Double(
      const std::size_t index,
      const containers::Double &obj
   ) {
      Double(index) = obj; return *this;
   }

   // Double(label,value)
   Q &Double(
      const std::string &label,
      const containers::Double &obj
   ) {
      Double(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Q() :
      Component{
         BodyText{},
         content.Double
      }
   {
      Component::finish();
   }

   // copy
   Q(const Q &other) :
      Component{
         other,
         content.Double
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Q(Q &&other) :
      Component{
         other,
         content.Double
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Q(const Node &node) :
      Component{
         BodyText{},
         content.Double
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Q(
      const std::vector<containers::Double> &Double
   ) :
      Component{
         BodyText{},
         content.Double
      },
      content{
         Double
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Q &operator=(const Q &) = default;

   // move
   Q &operator=(Q &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Q/src/custom.hpp"

}; // class Q

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
