
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_AVERAGEENERGY
#define NJOY_GNDSTK_V2_0_POPS_AVERAGEENERGY

// core interface
#include "GNDStk.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class AverageEnergy
// -----------------------------------------------------------------------------

namespace pops {

class AverageEnergy : public Component<AverageEnergy> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "AverageEnergy"; }
   static auto GNDSName() { return "averageEnergy"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("label") |
         XMLName{}
            / Meta<>("unit") |
         Float64{}
            / Meta<>("value")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName label;
      XMLName unit;
      Float64 value;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // unit
   const XMLName &unit() const
      { return content.unit; }
   XMLName &unit()
      { return content.unit; }

   // value
   const Float64 &value() const
      { return content.value; }
   Float64 &value()
      { return content.value; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // label(value)
   AverageEnergy &label(const XMLName &obj)
      { label() = obj; return *this; }

   // unit(value)
   AverageEnergy &unit(const XMLName &obj)
      { unit() = obj; return *this; }

   // value(value)
   AverageEnergy &value(const Float64 &obj)
      { value() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   AverageEnergy() :
      Component{
         BodyText{},
         content.label,
         content.unit,
         content.value
      }
   {
      Component::finish();
   }

   // copy
   AverageEnergy(const AverageEnergy &other) :
      Component{
         other,
         content.label,
         content.unit,
         content.value
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   AverageEnergy(AverageEnergy &&other) :
      Component{
         other,
         content.label,
         content.unit,
         content.value
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   AverageEnergy(const Node &node) :
      Component{
         BodyText{},
         content.label,
         content.unit,
         content.value
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit AverageEnergy(
      const XMLName &label,
      const XMLName &unit,
      const Float64 &value
   ) :
      Component{
         BodyText{},
         content.label,
         content.unit,
         content.value
      },
      content{
         label,
         unit,
         value
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   AverageEnergy &operator=(const AverageEnergy &) = default;

   // move
   AverageEnergy &operator=(AverageEnergy &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/AverageEnergy/src/custom.hpp"

}; // class AverageEnergy

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
