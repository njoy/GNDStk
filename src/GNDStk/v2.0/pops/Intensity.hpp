
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_INTENSITY
#define NJOY_GNDSTK_V2_0_POPS_INTENSITY

// core interface
#include "GNDStk.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Intensity
// -----------------------------------------------------------------------------

namespace pops {

class Intensity : public Component<Intensity> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Intensity"; }
   static auto GNDSName() { return "intensity"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<Float64>{}
            / Meta<>("value")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<Float64> value;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // value
   const std::optional<Float64> &value() const
      { return content.value; }
   std::optional<Float64> &value()
      { return content.value; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // value(value)
   Intensity &value(const std::optional<Float64> &obj)
      { value() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Intensity() :
      Component{
         BodyText{},
         content.value
      }
   {
      Component::finish();
   }

   // copy
   Intensity(const Intensity &other) :
      Component{
         other,
         content.value
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Intensity(Intensity &&other) :
      Component{
         other,
         content.value
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Intensity(const Node &node) :
      Component{
         BodyText{},
         content.value
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Intensity(
      const std::optional<Float64> &value
   ) :
      Component{
         BodyText{},
         content.value
      },
      content{
         value
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Intensity &operator=(const Intensity &) = default;

   // move
   Intensity &operator=(Intensity &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Intensity/src/custom.hpp"

}; // class Intensity

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
