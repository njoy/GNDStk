
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_BINDINGENERGY
#define NJOY_GNDSTK_V2_0_POPS_BINDINGENERGY

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Double.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class BindingEnergy
// -----------------------------------------------------------------------------

namespace pops {

class BindingEnergy : public Component<BindingEnergy> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "BindingEnergy"; }
   static auto GNDSName() { return "bindingEnergy"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         std::optional<containers::Double>{}
            / ++Child<>("Double")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::optional<std::vector<containers::Double>> Double;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // Double
   const std::optional<std::vector<containers::Double>> &Double() const
      { return content.Double; }
   std::optional<std::vector<containers::Double>> &Double()
      { return content.Double; }

   // Double(index)
   const containers::Double &Double(const std::size_t index) const
      { return getter(Double(), index, "Double"); }
   containers::Double &Double(const std::size_t index)
      { return getter(Double(), index, "Double"); }

   // Double(label)
   const containers::Double &Double(const std::string &label) const
      { return getter(Double(), label, "Double"); }
   containers::Double &Double(const std::string &label)
      { return getter(Double(), label, "Double"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // Double(value)
   BindingEnergy &Double(const std::optional<std::vector<containers::Double>> &obj)
      { Double() = obj; return *this; }

   // Double(index,value)
   BindingEnergy &Double(
      const std::size_t index,
      const containers::Double &obj
   ) {
      Double(index) = obj; return *this;
   }

   // Double(label,value)
   BindingEnergy &Double(
      const std::string &label,
      const containers::Double &obj
   ) {
      Double(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   BindingEnergy() :
      Component{
         BodyText{},
         content.Double
      }
   {
      Component::finish();
   }

   // copy
   BindingEnergy(const BindingEnergy &other) :
      Component{
         other,
         content.Double
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   BindingEnergy(BindingEnergy &&other) :
      Component{
         other,
         content.Double
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   BindingEnergy(const Node &node) :
      Component{
         BodyText{},
         content.Double
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit BindingEnergy(
      const std::optional<std::vector<containers::Double>> &Double
   ) :
      Component{
         BodyText{},
         content.Double
      },
      content{
         Double
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   BindingEnergy &operator=(const BindingEnergy &) = default;

   // move
   BindingEnergy &operator=(BindingEnergy &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/BindingEnergy/src/custom.hpp"

}; // class BindingEnergy

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
