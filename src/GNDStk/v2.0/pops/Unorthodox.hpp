
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_UNORTHODOX
#define NJOY_GNDSTK_V2_0_POPS_UNORTHODOX

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/pops/Charge.hpp"
#include "GNDStk/v2.0/pops/Mass.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Unorthodox
// -----------------------------------------------------------------------------

namespace pops {

class Unorthodox : public Component<Unorthodox> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Unorthodox"; }
   static auto GNDSName() { return "unorthodox"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("id") |
         // children
         std::optional<pops::Charge>{}
            / --Child<>("charge") |
         std::optional<pops::Mass>{}
            / --Child<>("mass")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName id;
      // children
      std::optional<pops::Charge> charge;
      std::optional<pops::Mass> mass;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // id
   const XMLName &id() const
      { return content.id; }
   XMLName &id()
      { return content.id; }

   // charge
   const std::optional<pops::Charge> &charge() const
      { return content.charge; }
   std::optional<pops::Charge> &charge()
      { return content.charge; }

   // mass
   const std::optional<pops::Mass> &mass() const
      { return content.mass; }
   std::optional<pops::Mass> &mass()
      { return content.mass; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // id(value)
   Unorthodox &id(const XMLName &obj)
      { id() = obj; return *this; }

   // charge(value)
   Unorthodox &charge(const std::optional<pops::Charge> &obj)
      { charge() = obj; return *this; }

   // mass(value)
   Unorthodox &mass(const std::optional<pops::Mass> &obj)
      { mass() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Unorthodox() :
      Component{
         BodyText{},
         content.id,
         content.charge,
         content.mass
      }
   {
      Component::finish();
   }

   // copy
   Unorthodox(const Unorthodox &other) :
      Component{
         other,
         content.id,
         content.charge,
         content.mass
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Unorthodox(Unorthodox &&other) :
      Component{
         other,
         content.id,
         content.charge,
         content.mass
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Unorthodox(const Node &node) :
      Component{
         BodyText{},
         content.id,
         content.charge,
         content.mass
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Unorthodox(
      const XMLName &id,
      const std::optional<pops::Charge> &charge,
      const std::optional<pops::Mass> &mass
   ) :
      Component{
         BodyText{},
         content.id,
         content.charge,
         content.mass
      },
      content{
         id,
         charge,
         mass
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Unorthodox &operator=(const Unorthodox &) = default;

   // move
   Unorthodox &operator=(Unorthodox &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Unorthodox/src/custom.hpp"

}; // class Unorthodox

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
