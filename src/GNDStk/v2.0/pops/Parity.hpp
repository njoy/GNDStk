
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_POPS_PARITY
#define NJOY_GNDSTK_V2_0_POPS_PARITY

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Integer.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// pops::
// class Parity
// -----------------------------------------------------------------------------

namespace pops {

class Parity : public Component<Parity> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "pops"; }
   static auto className() { return "Parity"; }
   static auto GNDSName() { return "parity"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         std::optional<containers::Integer>{}
            / ++Child<>("integer")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::optional<std::vector<containers::Integer>> integer;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // integer
   const std::optional<std::vector<containers::Integer>> &integer() const
      { return content.integer; }
   std::optional<std::vector<containers::Integer>> &integer()
      { return content.integer; }

   // integer(index)
   const containers::Integer &integer(const std::size_t index) const
      { return getter(integer(), index, "integer"); }
   containers::Integer &integer(const std::size_t index)
      { return getter(integer(), index, "integer"); }

   // integer(label)
   const containers::Integer &integer(const std::string &label) const
      { return getter(integer(), label, "integer"); }
   containers::Integer &integer(const std::string &label)
      { return getter(integer(), label, "integer"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // integer(value)
   Parity &integer(const std::optional<std::vector<containers::Integer>> &obj)
      { integer() = obj; return *this; }

   // integer(index,value)
   Parity &integer(
      const std::size_t index,
      const containers::Integer &obj
   ) {
      integer(index) = obj; return *this;
   }

   // integer(label,value)
   Parity &integer(
      const std::string &label,
      const containers::Integer &obj
   ) {
      integer(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Parity() :
      Component{
         BodyText{},
         content.integer
      }
   {
      Component::finish();
   }

   // copy
   Parity(const Parity &other) :
      Component{
         other,
         content.integer
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Parity(Parity &&other) :
      Component{
         other,
         content.integer
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Parity(const Node &node) :
      Component{
         BodyText{},
         content.integer
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Parity(
      const std::optional<std::vector<containers::Integer>> &integer
   ) :
      Component{
         BodyText{},
         content.integer
      },
      content{
         integer
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Parity &operator=(const Parity &) = default;

   // move
   Parity &operator=(Parity &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/pops/Parity/src/custom.hpp"

}; // class Parity

} // namespace pops
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
