
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_COVARIANCE_COVARIANCESECTIONS
#define NJOY_GNDSTK_V2_0_COVARIANCE_COVARIANCESECTIONS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/covariance/CovarianceSection.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// covariance::
// class CovarianceSections
// -----------------------------------------------------------------------------

namespace covariance {

class CovarianceSections : public Component<CovarianceSections> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "covariance"; }
   static auto className() { return "CovarianceSections"; }
   static auto GNDSName() { return "covarianceSections"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         std::optional<covariance::CovarianceSection>{}
            / ++Child<>("covarianceSection")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::optional<std::vector<covariance::CovarianceSection>> covarianceSection;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // covarianceSection
   const std::optional<std::vector<covariance::CovarianceSection>> &covarianceSection() const
      { return content.covarianceSection; }
   std::optional<std::vector<covariance::CovarianceSection>> &covarianceSection()
      { return content.covarianceSection; }

   // covarianceSection(index)
   const covariance::CovarianceSection &covarianceSection(const std::size_t index) const
      { return getter(covarianceSection(), index, "covarianceSection"); }
   covariance::CovarianceSection &covarianceSection(const std::size_t index)
      { return getter(covarianceSection(), index, "covarianceSection"); }

   // covarianceSection(label)
   const covariance::CovarianceSection &covarianceSection(const std::string &label) const
      { return getter(covarianceSection(), label, "covarianceSection"); }
   covariance::CovarianceSection &covarianceSection(const std::string &label)
      { return getter(covarianceSection(), label, "covarianceSection"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // covarianceSection(value)
   CovarianceSections &covarianceSection(const std::optional<std::vector<covariance::CovarianceSection>> &obj)
      { covarianceSection() = obj; return *this; }

   // covarianceSection(index,value)
   CovarianceSections &covarianceSection(
      const std::size_t index,
      const covariance::CovarianceSection &obj
   ) {
      covarianceSection(index) = obj; return *this;
   }

   // covarianceSection(label,value)
   CovarianceSections &covarianceSection(
      const std::string &label,
      const covariance::CovarianceSection &obj
   ) {
      covarianceSection(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   CovarianceSections() :
      Component{
         BodyText{},
         content.covarianceSection
      }
   {
      Component::finish();
   }

   // copy
   CovarianceSections(const CovarianceSections &other) :
      Component{
         other,
         content.covarianceSection
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   CovarianceSections(CovarianceSections &&other) :
      Component{
         other,
         content.covarianceSection
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   CovarianceSections(const Node &node) :
      Component{
         BodyText{},
         content.covarianceSection
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit CovarianceSections(
      const std::optional<std::vector<covariance::CovarianceSection>> &covarianceSection
   ) :
      Component{
         BodyText{},
         content.covarianceSection
      },
      content{
         covarianceSection
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   CovarianceSections &operator=(const CovarianceSections &) = default;

   // move
   CovarianceSections &operator=(CovarianceSections &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/covariance/CovarianceSections/src/custom.hpp"

}; // class CovarianceSections

} // namespace covariance
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
