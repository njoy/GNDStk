
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_COVARIANCE_AVERAGEPARAMETERCOVARIANCE
#define NJOY_GNDSTK_V2_0_COVARIANCE_AVERAGEPARAMETERCOVARIANCE

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/covariance/ColumnData.hpp"
#include "GNDStk/v2.0/covariance/RowData.hpp"
#include "GNDStk/v2.0/covariance/CovarianceMatrix.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// covariance::
// class AverageParameterCovariance
// -----------------------------------------------------------------------------

namespace covariance {

class AverageParameterCovariance : public Component<AverageParameterCovariance> {

   using _t = std::variant<
      covariance::CovarianceMatrix
   >;

   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "covariance"; }
   static auto className() { return "AverageParameterCovariance"; }
   static auto GNDSName() { return "averageParameterCovariance"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<bool>{}
            / Meta<>("crossTerm") |
         std::optional<XMLName>{}
            / Meta<>("label") |
         // children
         std::optional<covariance::ColumnData>{}
            / --Child<>("columnData") |
         std::optional<covariance::RowData>{}
            / --Child<>("rowData") |
         _t{}
            / --(Child<>("covarianceMatrix"))
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<bool> crossTerm;
      std::optional<XMLName> label;
      // children
      std::optional<covariance::ColumnData> columnData;
      std::optional<covariance::RowData> rowData;
      // children - variant
      _t _covarianceMatrix;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // crossTerm
   const std::optional<bool> &crossTerm() const
      { return content.crossTerm; }
   std::optional<bool> &crossTerm()
      { return content.crossTerm; }

   // label
   const std::optional<XMLName> &label() const
      { return content.label; }
   std::optional<XMLName> &label()
      { return content.label; }

   // columnData
   const std::optional<covariance::ColumnData> &columnData() const
      { return content.columnData; }
   std::optional<covariance::ColumnData> &columnData()
      { return content.columnData; }

   // rowData
   const std::optional<covariance::RowData> &rowData() const
      { return content.rowData; }
   std::optional<covariance::RowData> &rowData()
      { return content.rowData; }

   // _covarianceMatrix
   const _t &_covarianceMatrix() const
      { return content._covarianceMatrix; }
   _t &_covarianceMatrix()
      { return content._covarianceMatrix; }

   // covarianceMatrix
   const covariance::CovarianceMatrix *covarianceMatrix() const
      { return getter<covariance::CovarianceMatrix>(_covarianceMatrix(), "covarianceMatrix"); }
   covariance::CovarianceMatrix *covarianceMatrix()
      { return getter<covariance::CovarianceMatrix>(_covarianceMatrix(), "covarianceMatrix"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // crossTerm(value)
   AverageParameterCovariance &crossTerm(const std::optional<bool> &obj)
      { crossTerm() = obj; return *this; }

   // label(value)
   AverageParameterCovariance &label(const std::optional<XMLName> &obj)
      { label() = obj; return *this; }

   // columnData(value)
   AverageParameterCovariance &columnData(const std::optional<covariance::ColumnData> &obj)
      { columnData() = obj; return *this; }

   // rowData(value)
   AverageParameterCovariance &rowData(const std::optional<covariance::RowData> &obj)
      { rowData() = obj; return *this; }

   // _covarianceMatrix(value)
   AverageParameterCovariance &_covarianceMatrix(const _t &obj)
      { _covarianceMatrix() = obj; return *this; }

   // covarianceMatrix(value)
   AverageParameterCovariance &covarianceMatrix(const std::optional<covariance::CovarianceMatrix> &obj)
      { if (obj) _covarianceMatrix(obj.value()); return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   AverageParameterCovariance() :
      Component{
         BodyText{},
         content.crossTerm,
         content.label,
         content.columnData,
         content.rowData,
         content._covarianceMatrix
      }
   {
      Component::finish();
   }

   // copy
   AverageParameterCovariance(const AverageParameterCovariance &other) :
      Component{
         other,
         content.crossTerm,
         content.label,
         content.columnData,
         content.rowData,
         content._covarianceMatrix
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   AverageParameterCovariance(AverageParameterCovariance &&other) :
      Component{
         other,
         content.crossTerm,
         content.label,
         content.columnData,
         content.rowData,
         content._covarianceMatrix
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   AverageParameterCovariance(const Node &node) :
      Component{
         BodyText{},
         content.crossTerm,
         content.label,
         content.columnData,
         content.rowData,
         content._covarianceMatrix
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit AverageParameterCovariance(
      const std::optional<bool> &crossTerm,
      const std::optional<XMLName> &label,
      const std::optional<covariance::ColumnData> &columnData,
      const std::optional<covariance::RowData> &rowData,
      const _t &_covarianceMatrix
   ) :
      Component{
         BodyText{},
         content.crossTerm,
         content.label,
         content.columnData,
         content.rowData,
         content._covarianceMatrix
      },
      content{
         crossTerm,
         label,
         columnData,
         rowData,
         _covarianceMatrix
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   AverageParameterCovariance &operator=(const AverageParameterCovariance &) = default;

   // move
   AverageParameterCovariance &operator=(AverageParameterCovariance &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/covariance/AverageParameterCovariance/src/custom.hpp"

}; // class AverageParameterCovariance

} // namespace covariance
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
