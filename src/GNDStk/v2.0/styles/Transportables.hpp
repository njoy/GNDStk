
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_STYLES_TRANSPORTABLES
#define NJOY_GNDSTK_V2_0_STYLES_TRANSPORTABLES

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/styles/Transportable.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// styles::
// class Transportables
// -----------------------------------------------------------------------------

namespace styles {

class Transportables : public Component<Transportables> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "styles"; }
   static auto className() { return "Transportables"; }
   static auto GNDSName() { return "transportables"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         styles::Transportable{}
            / ++Child<>("transportable")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<styles::Transportable> transportable;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // transportable
   const std::vector<styles::Transportable> &transportable() const
      { return content.transportable; }
   std::vector<styles::Transportable> &transportable()
      { return content.transportable; }

   // transportable(index)
   const styles::Transportable &transportable(const std::size_t index) const
      { return getter(transportable(), index, "transportable"); }
   styles::Transportable &transportable(const std::size_t index)
      { return getter(transportable(), index, "transportable"); }

   // transportable(label)
   const styles::Transportable &transportable(const std::string &label) const
      { return getter(transportable(), label, "transportable"); }
   styles::Transportable &transportable(const std::string &label)
      { return getter(transportable(), label, "transportable"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // transportable(value)
   Transportables &transportable(const std::vector<styles::Transportable> &obj)
      { transportable() = obj; return *this; }

   // transportable(index,value)
   Transportables &transportable(
      const std::size_t index,
      const styles::Transportable &obj
   ) {
      transportable(index) = obj; return *this;
   }

   // transportable(label,value)
   Transportables &transportable(
      const std::string &label,
      const styles::Transportable &obj
   ) {
      transportable(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Transportables() :
      Component{
         BodyText{},
         content.transportable
      }
   {
      Component::finish();
   }

   // copy
   Transportables(const Transportables &other) :
      Component{
         other,
         content.transportable
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Transportables(Transportables &&other) :
      Component{
         other,
         content.transportable
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Transportables(const Node &node) :
      Component{
         BodyText{},
         content.transportable
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Transportables(
      const std::vector<styles::Transportable> &transportable
   ) :
      Component{
         BodyText{},
         content.transportable
      },
      content{
         transportable
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Transportables &operator=(const Transportables &) = default;

   // move
   Transportables &operator=(Transportables &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/styles/Transportables/src/custom.hpp"

}; // class Transportables

} // namespace styles
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
