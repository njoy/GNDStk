
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_STYLES_GRIDDEDCROSSSECTION
#define NJOY_GNDSTK_V2_0_STYLES_GRIDDEDCROSSSECTION

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/documentation/Documentation.hpp"
#include "GNDStk/v2.0/containers/Grid.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// styles::
// class GriddedCrossSection
// -----------------------------------------------------------------------------

namespace styles {

class GriddedCrossSection : public Component<GriddedCrossSection> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "styles"; }
   static auto className() { return "GriddedCrossSection"; }
   static auto GNDSName() { return "griddedCrossSection"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::string{}
            / Meta<>("date") |
         XMLName{}
            / Meta<>("derivedFrom") |
         XMLName{}
            / Meta<>("label") |
         // children
         std::optional<documentation::Documentation>{}
            / --Child<>("documentation") |
         containers::Grid{}
            / --Child<>("grid")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::string date;
      XMLName derivedFrom;
      XMLName label;
      // children
      std::optional<documentation::Documentation> documentation;
      containers::Grid grid;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // date
   const std::string &date() const
      { return content.date; }
   std::string &date()
      { return content.date; }

   // derivedFrom
   const XMLName &derivedFrom() const
      { return content.derivedFrom; }
   XMLName &derivedFrom()
      { return content.derivedFrom; }

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // documentation
   const std::optional<documentation::Documentation> &documentation() const
      { return content.documentation; }
   std::optional<documentation::Documentation> &documentation()
      { return content.documentation; }

   // grid
   const containers::Grid &grid() const
      { return content.grid; }
   containers::Grid &grid()
      { return content.grid; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // date(value)
   GriddedCrossSection &date(const std::string &obj)
      { date() = obj; return *this; }

   // derivedFrom(value)
   GriddedCrossSection &derivedFrom(const XMLName &obj)
      { derivedFrom() = obj; return *this; }

   // label(value)
   GriddedCrossSection &label(const XMLName &obj)
      { label() = obj; return *this; }

   // documentation(value)
   GriddedCrossSection &documentation(const std::optional<documentation::Documentation> &obj)
      { documentation() = obj; return *this; }

   // grid(value)
   GriddedCrossSection &grid(const containers::Grid &obj)
      { grid() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   GriddedCrossSection() :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.grid
      }
   {
      Component::finish();
   }

   // copy
   GriddedCrossSection(const GriddedCrossSection &other) :
      Component{
         other,
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.grid
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   GriddedCrossSection(GriddedCrossSection &&other) :
      Component{
         other,
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.grid
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   GriddedCrossSection(const Node &node) :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.grid
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit GriddedCrossSection(
      const std::string &date,
      const XMLName &derivedFrom,
      const XMLName &label,
      const std::optional<documentation::Documentation> &documentation,
      const containers::Grid &grid
   ) :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.grid
      },
      content{
         date,
         derivedFrom,
         label,
         documentation,
         grid
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   GriddedCrossSection &operator=(const GriddedCrossSection &) = default;

   // move
   GriddedCrossSection &operator=(GriddedCrossSection &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/styles/GriddedCrossSection/src/custom.hpp"

}; // class GriddedCrossSection

} // namespace styles
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
