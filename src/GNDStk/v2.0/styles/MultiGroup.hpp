
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_STYLES_MULTIGROUP
#define NJOY_GNDSTK_V2_0_STYLES_MULTIGROUP

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/containers/Grid.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// styles::
// class MultiGroup
// -----------------------------------------------------------------------------

namespace styles {

class MultiGroup : public Component<MultiGroup> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "styles"; }
   static auto className() { return "MultiGroup"; }
   static auto GNDSName() { return "multiGroup"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("label") |
         // children
         containers::Grid{}
            / --Child<>("grid")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName label;
      // children
      containers::Grid grid;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // grid
   const containers::Grid &grid() const
      { return content.grid; }
   containers::Grid &grid()
      { return content.grid; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // label(value)
   MultiGroup &label(const XMLName &obj)
      { label() = obj; return *this; }

   // grid(value)
   MultiGroup &grid(const containers::Grid &obj)
      { grid() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   MultiGroup() :
      Component{
         BodyText{},
         content.label,
         content.grid
      }
   {
      Component::finish();
   }

   // copy
   MultiGroup(const MultiGroup &other) :
      Component{
         other,
         content.label,
         content.grid
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   MultiGroup(MultiGroup &&other) :
      Component{
         other,
         content.label,
         content.grid
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   MultiGroup(const Node &node) :
      Component{
         BodyText{},
         content.label,
         content.grid
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit MultiGroup(
      const XMLName &label,
      const containers::Grid &grid
   ) :
      Component{
         BodyText{},
         content.label,
         content.grid
      },
      content{
         label,
         grid
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   MultiGroup &operator=(const MultiGroup &) = default;

   // move
   MultiGroup &operator=(MultiGroup &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/styles/MultiGroup/src/custom.hpp"

}; // class MultiGroup

} // namespace styles
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
