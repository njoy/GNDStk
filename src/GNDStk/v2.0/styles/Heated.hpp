
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_STYLES_HEATED
#define NJOY_GNDSTK_V2_0_STYLES_HEATED

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/documentation/Documentation.hpp"
#include "GNDStk/v2.0/styles/Temperature.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// styles::
// class Heated
// -----------------------------------------------------------------------------

namespace styles {

class Heated : public Component<Heated> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "styles"; }
   static auto className() { return "Heated"; }
   static auto GNDSName() { return "heated"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::string{}
            / Meta<>("date") |
         XMLName{}
            / Meta<>("derivedFrom") |
         XMLName{}
            / Meta<>("label") |
         // children
         std::optional<documentation::Documentation>{}
            / --Child<>("documentation") |
         styles::Temperature{}
            / --Child<>("temperature")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::string date;
      XMLName derivedFrom;
      XMLName label;
      // children
      std::optional<documentation::Documentation> documentation;
      styles::Temperature temperature;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // date
   const std::string &date() const
      { return content.date; }
   std::string &date()
      { return content.date; }

   // derivedFrom
   const XMLName &derivedFrom() const
      { return content.derivedFrom; }
   XMLName &derivedFrom()
      { return content.derivedFrom; }

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // documentation
   const std::optional<documentation::Documentation> &documentation() const
      { return content.documentation; }
   std::optional<documentation::Documentation> &documentation()
      { return content.documentation; }

   // temperature
   const styles::Temperature &temperature() const
      { return content.temperature; }
   styles::Temperature &temperature()
      { return content.temperature; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // date(value)
   Heated &date(const std::string &obj)
      { date() = obj; return *this; }

   // derivedFrom(value)
   Heated &derivedFrom(const XMLName &obj)
      { derivedFrom() = obj; return *this; }

   // label(value)
   Heated &label(const XMLName &obj)
      { label() = obj; return *this; }

   // documentation(value)
   Heated &documentation(const std::optional<documentation::Documentation> &obj)
      { documentation() = obj; return *this; }

   // temperature(value)
   Heated &temperature(const styles::Temperature &obj)
      { temperature() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Heated() :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.temperature
      }
   {
      Component::finish();
   }

   // copy
   Heated(const Heated &other) :
      Component{
         other,
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.temperature
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Heated(Heated &&other) :
      Component{
         other,
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.temperature
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Heated(const Node &node) :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.temperature
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Heated(
      const std::string &date,
      const XMLName &derivedFrom,
      const XMLName &label,
      const std::optional<documentation::Documentation> &documentation,
      const styles::Temperature &temperature
   ) :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.documentation,
         content.temperature
      },
      content{
         date,
         derivedFrom,
         label,
         documentation,
         temperature
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Heated &operator=(const Heated &) = default;

   // move
   Heated &operator=(Heated &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/styles/Heated/src/custom.hpp"

}; // class Heated

} // namespace styles
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
