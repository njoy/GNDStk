
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_STYLES_SNELASTICUPSCATTER
#define NJOY_GNDSTK_V2_0_STYLES_SNELASTICUPSCATTER

// core interface
#include "GNDStk.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// styles::
// class SnElasticUpScatter
// -----------------------------------------------------------------------------

namespace styles {

class SnElasticUpScatter : public Component<SnElasticUpScatter> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "styles"; }
   static auto className() { return "SnElasticUpScatter"; }
   static auto GNDSName() { return "SnElasticUpScatter"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::string{}
            / Meta<>("date") |
         XMLName{}
            / Meta<>("derivedFrom") |
         XMLName{}
            / Meta<>("label") |
         std::optional<Integer32>{}
            / Meta<>("upperCalculatedGroup")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::string date;
      XMLName derivedFrom;
      XMLName label;
      std::optional<Integer32> upperCalculatedGroup;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // date
   const std::string &date() const
      { return content.date; }
   std::string &date()
      { return content.date; }

   // derivedFrom
   const XMLName &derivedFrom() const
      { return content.derivedFrom; }
   XMLName &derivedFrom()
      { return content.derivedFrom; }

   // label
   const XMLName &label() const
      { return content.label; }
   XMLName &label()
      { return content.label; }

   // upperCalculatedGroup
   const std::optional<Integer32> &upperCalculatedGroup() const
      { return content.upperCalculatedGroup; }
   std::optional<Integer32> &upperCalculatedGroup()
      { return content.upperCalculatedGroup; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // date(value)
   SnElasticUpScatter &date(const std::string &obj)
      { date() = obj; return *this; }

   // derivedFrom(value)
   SnElasticUpScatter &derivedFrom(const XMLName &obj)
      { derivedFrom() = obj; return *this; }

   // label(value)
   SnElasticUpScatter &label(const XMLName &obj)
      { label() = obj; return *this; }

   // upperCalculatedGroup(value)
   SnElasticUpScatter &upperCalculatedGroup(const std::optional<Integer32> &obj)
      { upperCalculatedGroup() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   SnElasticUpScatter() :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.upperCalculatedGroup
      }
   {
      Component::finish();
   }

   // copy
   SnElasticUpScatter(const SnElasticUpScatter &other) :
      Component{
         other,
         content.date,
         content.derivedFrom,
         content.label,
         content.upperCalculatedGroup
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   SnElasticUpScatter(SnElasticUpScatter &&other) :
      Component{
         other,
         content.date,
         content.derivedFrom,
         content.label,
         content.upperCalculatedGroup
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   SnElasticUpScatter(const Node &node) :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.upperCalculatedGroup
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit SnElasticUpScatter(
      const std::string &date,
      const XMLName &derivedFrom,
      const XMLName &label,
      const std::optional<Integer32> &upperCalculatedGroup
   ) :
      Component{
         BodyText{},
         content.date,
         content.derivedFrom,
         content.label,
         content.upperCalculatedGroup
      },
      content{
         date,
         derivedFrom,
         label,
         upperCalculatedGroup
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   SnElasticUpScatter &operator=(const SnElasticUpScatter &) = default;

   // move
   SnElasticUpScatter &operator=(SnElasticUpScatter &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/styles/SnElasticUpScatter/src/custom.hpp"

}; // class SnElasticUpScatter

} // namespace styles
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
