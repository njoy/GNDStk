
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_DOCUMENTATION_CODEREPO
#define NJOY_GNDSTK_V2_0_DOCUMENTATION_CODEREPO

// core interface
#include "GNDStk.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// documentation::
// class CodeRepo
// -----------------------------------------------------------------------------

namespace documentation {

class CodeRepo : public Component<CodeRepo> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "documentation"; }
   static auto className() { return "CodeRepo"; }
   static auto GNDSName() { return "codeRepo"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         XMLName{}
            / Meta<>("href") |
         XMLName{}
            / Meta<>("revisionID") |
         XMLName{}
            / Meta<>("revisionSystem")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      XMLName href;
      XMLName revisionID;
      XMLName revisionSystem;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // href
   const XMLName &href() const
      { return content.href; }
   XMLName &href()
      { return content.href; }

   // revisionID
   const XMLName &revisionID() const
      { return content.revisionID; }
   XMLName &revisionID()
      { return content.revisionID; }

   // revisionSystem
   const XMLName &revisionSystem() const
      { return content.revisionSystem; }
   XMLName &revisionSystem()
      { return content.revisionSystem; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // href(value)
   CodeRepo &href(const XMLName &obj)
      { href() = obj; return *this; }

   // revisionID(value)
   CodeRepo &revisionID(const XMLName &obj)
      { revisionID() = obj; return *this; }

   // revisionSystem(value)
   CodeRepo &revisionSystem(const XMLName &obj)
      { revisionSystem() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   CodeRepo() :
      Component{
         BodyText{},
         content.href,
         content.revisionID,
         content.revisionSystem
      }
   {
      Component::finish();
   }

   // copy
   CodeRepo(const CodeRepo &other) :
      Component{
         other,
         content.href,
         content.revisionID,
         content.revisionSystem
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   CodeRepo(CodeRepo &&other) :
      Component{
         other,
         content.href,
         content.revisionID,
         content.revisionSystem
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   CodeRepo(const Node &node) :
      Component{
         BodyText{},
         content.href,
         content.revisionID,
         content.revisionSystem
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit CodeRepo(
      const XMLName &href,
      const XMLName &revisionID,
      const XMLName &revisionSystem
   ) :
      Component{
         BodyText{},
         content.href,
         content.revisionID,
         content.revisionSystem
      },
      content{
         href,
         revisionID,
         revisionSystem
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   CodeRepo &operator=(const CodeRepo &) = default;

   // move
   CodeRepo &operator=(CodeRepo &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/documentation/CodeRepo/src/custom.hpp"

}; // class CodeRepo

} // namespace documentation
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
