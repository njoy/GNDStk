
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_DOCUMENTATION_AFFILIATION
#define NJOY_GNDSTK_V2_0_DOCUMENTATION_AFFILIATION

// core interface
#include "GNDStk.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// documentation::
// class Affiliation
// -----------------------------------------------------------------------------

namespace documentation {

class Affiliation : public Component<Affiliation> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "documentation"; }
   static auto className() { return "Affiliation"; }
   static auto GNDSName() { return "affiliation"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // metadata
         std::optional<UTF8Text>{}
            / Meta<>("href") |
         UTF8Text{}
            / Meta<>("name")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // metadata
      std::optional<UTF8Text> href;
      UTF8Text name;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // href
   const std::optional<UTF8Text> &href() const
      { return content.href; }
   std::optional<UTF8Text> &href()
      { return content.href; }

   // name
   const UTF8Text &name() const
      { return content.name; }
   UTF8Text &name()
      { return content.name; }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // href(value)
   Affiliation &href(const std::optional<UTF8Text> &obj)
      { href() = obj; return *this; }

   // name(value)
   Affiliation &name(const UTF8Text &obj)
      { name() = obj; return *this; }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Affiliation() :
      Component{
         BodyText{},
         content.href,
         content.name
      }
   {
      Component::finish();
   }

   // copy
   Affiliation(const Affiliation &other) :
      Component{
         other,
         content.href,
         content.name
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Affiliation(Affiliation &&other) :
      Component{
         other,
         content.href,
         content.name
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Affiliation(const Node &node) :
      Component{
         BodyText{},
         content.href,
         content.name
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Affiliation(
      const std::optional<UTF8Text> &href,
      const UTF8Text &name
   ) :
      Component{
         BodyText{},
         content.href,
         content.name
      },
      content{
         href,
         name
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Affiliation &operator=(const Affiliation &) = default;

   // move
   Affiliation &operator=(Affiliation &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/documentation/Affiliation/src/custom.hpp"

}; // class Affiliation

} // namespace documentation
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
