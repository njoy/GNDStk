
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_DOCUMENTATION_ACKNOWLEDGEMENTS
#define NJOY_GNDSTK_V2_0_DOCUMENTATION_ACKNOWLEDGEMENTS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/documentation/Acknowledgement.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// documentation::
// class Acknowledgements
// -----------------------------------------------------------------------------

namespace documentation {

class Acknowledgements : public Component<Acknowledgements> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "documentation"; }
   static auto className() { return "Acknowledgements"; }
   static auto GNDSName() { return "acknowledgements"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         documentation::Acknowledgement{}
            / ++Child<>("acknowledgement")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<documentation::Acknowledgement> acknowledgement;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // acknowledgement
   const std::vector<documentation::Acknowledgement> &acknowledgement() const
      { return content.acknowledgement; }
   std::vector<documentation::Acknowledgement> &acknowledgement()
      { return content.acknowledgement; }

   // acknowledgement(index)
   const documentation::Acknowledgement &acknowledgement(const std::size_t index) const
      { return getter(acknowledgement(), index, "acknowledgement"); }
   documentation::Acknowledgement &acknowledgement(const std::size_t index)
      { return getter(acknowledgement(), index, "acknowledgement"); }

   // acknowledgement(label)
   const documentation::Acknowledgement &acknowledgement(const std::string &label) const
      { return getter(acknowledgement(), label, "acknowledgement"); }
   documentation::Acknowledgement &acknowledgement(const std::string &label)
      { return getter(acknowledgement(), label, "acknowledgement"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // acknowledgement(value)
   Acknowledgements &acknowledgement(const std::vector<documentation::Acknowledgement> &obj)
      { acknowledgement() = obj; return *this; }

   // acknowledgement(index,value)
   Acknowledgements &acknowledgement(
      const std::size_t index,
      const documentation::Acknowledgement &obj
   ) {
      acknowledgement(index) = obj; return *this;
   }

   // acknowledgement(label,value)
   Acknowledgements &acknowledgement(
      const std::string &label,
      const documentation::Acknowledgement &obj
   ) {
      acknowledgement(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Acknowledgements() :
      Component{
         BodyText{},
         content.acknowledgement
      }
   {
      Component::finish();
   }

   // copy
   Acknowledgements(const Acknowledgements &other) :
      Component{
         other,
         content.acknowledgement
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Acknowledgements(Acknowledgements &&other) :
      Component{
         other,
         content.acknowledgement
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Acknowledgements(const Node &node) :
      Component{
         BodyText{},
         content.acknowledgement
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Acknowledgements(
      const std::vector<documentation::Acknowledgement> &acknowledgement
   ) :
      Component{
         BodyText{},
         content.acknowledgement
      },
      content{
         acknowledgement
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Acknowledgements &operator=(const Acknowledgements &) = default;

   // move
   Acknowledgements &operator=(Acknowledgements &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/documentation/Acknowledgements/src/custom.hpp"

}; // class Acknowledgements

} // namespace documentation
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
