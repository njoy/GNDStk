
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_DOCUMENTATION_RELATEDITEMS
#define NJOY_GNDSTK_V2_0_DOCUMENTATION_RELATEDITEMS

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/documentation/RelatedItem.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// documentation::
// class RelatedItems
// -----------------------------------------------------------------------------

namespace documentation {

class RelatedItems : public Component<RelatedItems> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "documentation"; }
   static auto className() { return "RelatedItems"; }
   static auto GNDSName() { return "relatedItems"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         documentation::RelatedItem{}
            / ++Child<>("relatedItem")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<documentation::RelatedItem> relatedItem;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // relatedItem
   const std::vector<documentation::RelatedItem> &relatedItem() const
      { return content.relatedItem; }
   std::vector<documentation::RelatedItem> &relatedItem()
      { return content.relatedItem; }

   // relatedItem(index)
   const documentation::RelatedItem &relatedItem(const std::size_t index) const
      { return getter(relatedItem(), index, "relatedItem"); }
   documentation::RelatedItem &relatedItem(const std::size_t index)
      { return getter(relatedItem(), index, "relatedItem"); }

   // relatedItem(label)
   const documentation::RelatedItem &relatedItem(const std::string &label) const
      { return getter(relatedItem(), label, "relatedItem"); }
   documentation::RelatedItem &relatedItem(const std::string &label)
      { return getter(relatedItem(), label, "relatedItem"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // relatedItem(value)
   RelatedItems &relatedItem(const std::vector<documentation::RelatedItem> &obj)
      { relatedItem() = obj; return *this; }

   // relatedItem(index,value)
   RelatedItems &relatedItem(
      const std::size_t index,
      const documentation::RelatedItem &obj
   ) {
      relatedItem(index) = obj; return *this;
   }

   // relatedItem(label,value)
   RelatedItems &relatedItem(
      const std::string &label,
      const documentation::RelatedItem &obj
   ) {
      relatedItem(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   RelatedItems() :
      Component{
         BodyText{},
         content.relatedItem
      }
   {
      Component::finish();
   }

   // copy
   RelatedItems(const RelatedItems &other) :
      Component{
         other,
         content.relatedItem
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   RelatedItems(RelatedItems &&other) :
      Component{
         other,
         content.relatedItem
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   RelatedItems(const Node &node) :
      Component{
         BodyText{},
         content.relatedItem
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit RelatedItems(
      const std::vector<documentation::RelatedItem> &relatedItem
   ) :
      Component{
         BodyText{},
         content.relatedItem
      },
      content{
         relatedItem
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   RelatedItems &operator=(const RelatedItems &) = default;

   // move
   RelatedItems &operator=(RelatedItems &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/documentation/RelatedItems/src/custom.hpp"

}; // class RelatedItems

} // namespace documentation
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
