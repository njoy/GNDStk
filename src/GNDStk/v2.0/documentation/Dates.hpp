
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef NJOY_GNDSTK_V2_0_DOCUMENTATION_DATES
#define NJOY_GNDSTK_V2_0_DOCUMENTATION_DATES

// core interface
#include "GNDStk.hpp"

// v2.0 dependencies
#include "GNDStk/v2.0/documentation/Date.hpp"

namespace njoy {
namespace GNDStk {
namespace v2_0 {

using namespace njoy::GNDStk::core;



// -----------------------------------------------------------------------------
// documentation::
// class Dates
// -----------------------------------------------------------------------------

namespace documentation {

class Dates : public Component<Dates> {


   // ------------------------
   // For Component
   // ------------------------

   friend class Component;

   // Current namespace, current class, and GNDS node name
   static auto namespaceName() { return "documentation"; }
   static auto className() { return "Dates"; }
   static auto GNDSName() { return "dates"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto keys()
   {
      return
         // children
         documentation::Date{}
            / ++Child<>("date")
      ;
   }

public:

   using Component::construct;

   // ------------------------
   // Relevant defaults
   // FYI for users
   // ------------------------

   static inline const struct Defaults {
   } defaults;

   // ------------------------
   // Raw GNDS content
   // ------------------------

   struct {
      // children
      std::vector<documentation::Date> date;
   } content;

   // ------------------------
   // Getters
   // const and non-const
   // ------------------------

   // date
   const std::vector<documentation::Date> &date() const
      { return content.date; }
   std::vector<documentation::Date> &date()
      { return content.date; }

   // date(index)
   const documentation::Date &date(const std::size_t index) const
      { return getter(date(), index, "date"); }
   documentation::Date &date(const std::size_t index)
      { return getter(date(), index, "date"); }

   // date(label)
   const documentation::Date &date(const std::string &label) const
      { return getter(date(), label, "date"); }
   documentation::Date &date(const std::string &label)
      { return getter(date(), label, "date"); }

   // ------------------------
   // Setters
   // non-const
   // All return *this
   // ------------------------

   // date(value)
   Dates &date(const std::vector<documentation::Date> &obj)
      { date() = obj; return *this; }

   // date(index,value)
   Dates &date(
      const std::size_t index,
      const documentation::Date &obj
   ) {
      date(index) = obj; return *this;
   }

   // date(label,value)
   Dates &date(
      const std::string &label,
      const documentation::Date &obj
   ) {
      date(label) = obj; return *this;
   }

   // ------------------------
   // Construction
   // ------------------------

   // default
   Dates() :
      Component{
         BodyText{},
         content.date
      }
   {
      Component::finish();
   }

   // copy
   Dates(const Dates &other) :
      Component{
         other,
         content.date
      },
      content{other.content}
   {
      Component::finish(other);
   }

   // move
   Dates(Dates &&other) :
      Component{
         other,
         content.date
      },
      content{std::move(other.content)}
   {
      Component::finish(other);
   }

   // from node
   Dates(const Node &node) :
      Component{
         BodyText{},
         content.date
      }
   {
      Component::finish(node);
   }

   // from fields
   explicit Dates(
      const std::vector<documentation::Date> &date
   ) :
      Component{
         BodyText{},
         content.date
      },
      content{
         date
      }
   {
      Component::finish();
   }

   // ------------------------
   // Assignment
   // ------------------------

   // copy
   Dates &operator=(const Dates &) = default;

   // move
   Dates &operator=(Dates &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "GNDStk/v2.0/documentation/Dates/src/custom.hpp"

}; // class Dates

} // namespace documentation
} // namespace v2_0
} // namespace GNDStk
} // namespace njoy

#endif
