
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_COVARIANCE_SLICES
#define ALPHA_V2_0_COVARIANCE_SLICES

#include "alpha/v2.0/covariance/Slice.hpp"

namespace alpha {
namespace v2_0 {
namespace covariance {

// -----------------------------------------------------------------------------
// covariance::
// class Slices
// -----------------------------------------------------------------------------

class Slices :
   public Component<covariance::Slices>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "covariance"; }
   static auto CLASS() { return "Slices"; }
   static auto FIELD() { return "slices"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<covariance::Slice>
            ("slice")
      ;
   }

public:
   using Component::construct;

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<covariance::Slice>>
      slice{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->comment, \
      this->slice)

   // default
   Slices() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Slices(
      const wrapper<std::vector<covariance::Slice>>
         &slice
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      slice(this,slice)
   {
      Component::finish();
   }

   // from node
   explicit Slices(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Slices(const Slices &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      slice(this,other.slice)
   {
      Component::finish(other);
   }

   // move
   Slices(Slices &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      slice(this,std::move(other.slice))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   Slices &operator=(const Slices &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         slice = other.slice;
      }
      return *this;
   }

   // move
   Slices &operator=(Slices &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         slice = std::move(other.slice);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/covariance/Slices/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Slices

} // namespace covariance
} // namespace v2_0
} // namespace alpha

#endif
