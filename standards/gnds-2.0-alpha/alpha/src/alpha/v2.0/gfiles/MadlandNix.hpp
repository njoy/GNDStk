
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_GFILES_MADLANDNIX
#define ALPHA_V2_0_GFILES_MADLANDNIX

#include "alpha/v2.0/gfiles/EFL.hpp"
#include "alpha/v2.0/gfiles/EFH.hpp"
#include "alpha/v2.0/gfiles/T_M.hpp"

namespace alpha {
namespace v2_0 {
namespace gfiles {

// -----------------------------------------------------------------------------
// gfiles::
// class MadlandNix
// -----------------------------------------------------------------------------

class MadlandNix :
   public Component<gfiles::MadlandNix>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "gfiles"; }
   static auto CLASS() { return "MadlandNix"; }
   static auto FIELD() { return "MadlandNix"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         --Child<gfiles::EFL>("EFL") |
         --Child<gfiles::EFH>("EFH") |
         --Child<gfiles::T_M>("T_M")
      ;
   }

public:
   using Component::construct;

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<gfiles::EFL> EFL{this};
   Field<gfiles::EFH> EFH{this};
   Field<gfiles::T_M> T_M{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->comment, \
      this->EFL, \
      this->EFH, \
      this->T_M)

   // default
   MadlandNix() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit MadlandNix(
      const wrapper<gfiles::EFL> &EFL,
      const wrapper<gfiles::EFH> &EFH = {},
      const wrapper<gfiles::T_M> &T_M = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      EFL(this,EFL),
      EFH(this,EFH),
      T_M(this,T_M)
   {
      Component::finish();
   }

   // from node
   explicit MadlandNix(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   MadlandNix(const MadlandNix &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      EFL(this,other.EFL),
      EFH(this,other.EFH),
      T_M(this,other.T_M)
   {
      Component::finish(other);
   }

   // move
   MadlandNix(MadlandNix &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      EFL(this,std::move(other.EFL)),
      EFH(this,std::move(other.EFH)),
      T_M(this,std::move(other.T_M))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   MadlandNix &operator=(const MadlandNix &) = default;
   MadlandNix &operator=(MadlandNix &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/gfiles/MadlandNix/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class MadlandNix

} // namespace gfiles
} // namespace v2_0
} // namespace alpha

#endif
