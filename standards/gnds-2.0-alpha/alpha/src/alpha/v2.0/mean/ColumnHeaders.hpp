
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_MEAN_COLUMNHEADERS
#define ALPHA_V2_0_MEAN_COLUMNHEADERS

#include "alpha/v2.0/mean/Column.hpp"

namespace alpha {
namespace v2_0 {
namespace mean {

// -----------------------------------------------------------------------------
// mean::
// class ColumnHeaders
// -----------------------------------------------------------------------------

class ColumnHeaders :
   public Component<mean::ColumnHeaders>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "mean"; }
   static auto CLASS() { return "ColumnHeaders"; }
   static auto FIELD() { return "columnHeaders"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<mean::Column>
            ("column")
      ;
   }

public:
   using Component::construct;

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<mean::Column>>
      column{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->comment, \
      this->column)

   // default
   ColumnHeaders() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit ColumnHeaders(
      const wrapper<std::vector<mean::Column>>
         &column
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      column(this,column)
   {
      Component::finish();
   }

   // from node
   explicit ColumnHeaders(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   ColumnHeaders(const ColumnHeaders &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      column(this,other.column)
   {
      Component::finish(other);
   }

   // move
   ColumnHeaders(ColumnHeaders &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      column(this,std::move(other.column))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   ColumnHeaders &operator=(const ColumnHeaders &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         column = other.column;
      }
      return *this;
   }

   // move
   ColumnHeaders &operator=(ColumnHeaders &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         column = std::move(other.column);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/mean/ColumnHeaders/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class ColumnHeaders

} // namespace mean
} // namespace v2_0
} // namespace alpha

#endif
