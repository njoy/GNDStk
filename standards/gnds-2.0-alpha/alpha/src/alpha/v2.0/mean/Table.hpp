
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_MEAN_TABLE
#define ALPHA_V2_0_MEAN_TABLE

#include "alpha/v2.0/mean/ColumnHeaders.hpp"
#include "alpha/v2.0/mean/Data.hpp"

namespace alpha {
namespace v2_0 {
namespace mean {

// -----------------------------------------------------------------------------
// mean::
// class Table
// -----------------------------------------------------------------------------

class Table :
   public Component<mean::Table>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "mean"; }
   static auto CLASS() { return "Table"; }
   static auto FIELD() { return "table"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // metadata
         int{}
            / Meta<>("rows") |
         int{}
            / Meta<>("columns") |

         // children
         --Child<mean::ColumnHeaders>("columnHeaders") |
         --Child<mean::Data>("data")
      ;
   }

public:
   using Component::construct;

   // comment
   Field<std::vector<std::string>> comment{this};

   // metadata
   Field<int> rows{this};
   Field<int> columns{this};

   // children
   Field<mean::ColumnHeaders> columnHeaders{this};
   Field<mean::Data> data{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->comment, \
      this->rows, \
      this->columns, \
      this->columnHeaders, \
      this->data)

   // default
   Table() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Table(
      const wrapper<int> &rows,
      const wrapper<int> &columns = {},
      const wrapper<mean::ColumnHeaders> &columnHeaders = {},
      const wrapper<mean::Data> &data = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      rows(this,rows),
      columns(this,columns),
      columnHeaders(this,columnHeaders),
      data(this,data)
   {
      Component::finish();
   }

   // from node
   explicit Table(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Table(const Table &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      rows(this,other.rows),
      columns(this,other.columns),
      columnHeaders(this,other.columnHeaders),
      data(this,other.data)
   {
      Component::finish(other);
   }

   // move
   Table(Table &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      rows(this,std::move(other.rows)),
      columns(this,std::move(other.columns)),
      columnHeaders(this,std::move(other.columnHeaders)),
      data(this,std::move(other.data))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   Table &operator=(const Table &) = default;
   Table &operator=(Table &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/mean/Table/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Table

} // namespace mean
} // namespace v2_0
} // namespace alpha

#endif
