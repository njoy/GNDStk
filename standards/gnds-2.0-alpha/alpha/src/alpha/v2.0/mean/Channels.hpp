
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_MEAN_CHANNELS
#define ALPHA_V2_0_MEAN_CHANNELS

#include "alpha/v2.0/mean/Channel.hpp"

namespace alpha {
namespace v2_0 {
namespace mean {

// -----------------------------------------------------------------------------
// mean::
// class Channels
// -----------------------------------------------------------------------------

class Channels :
   public Component<mean::Channels>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "mean"; }
   static auto CLASS() { return "Channels"; }
   static auto FIELD() { return "channels"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<mean::Channel>("channel")
      ;
   }

public:
   using Component::construct;

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<mean::Channel>> channel{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->comment, \
      this->channel)

   // default
   Channels() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Channels(
      const wrapper<std::vector<mean::Channel>> &channel
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      channel(this,channel)
   {
      Component::finish();
   }

   // from node
   explicit Channels(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Channels(const Channels &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      channel(this,other.channel)
   {
      Component::finish(other);
   }

   // move
   Channels(Channels &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      channel(this,std::move(other.channel))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   Channels &operator=(const Channels &) = default;
   Channels &operator=(Channels &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/mean/Channels/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Channels

} // namespace mean
} // namespace v2_0
} // namespace alpha

#endif
