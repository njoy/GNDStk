
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_COMMON_UNCERTAINTY
#define ALPHA_V2_0_COMMON_UNCERTAINTY

#include "alpha/v2.0/common/Covariance.hpp"
#include "alpha/v2.0/sfy/Standard.hpp"
#include "alpha/v2.0/mean/ListOfCovariances.hpp"
#include "alpha/v2.0/reduced/Polynomial1d.hpp"

namespace alpha {
namespace v2_0 {
namespace common {

// -----------------------------------------------------------------------------
// common::
// class Uncertainty
// -----------------------------------------------------------------------------

class Uncertainty :
   public Component<common::Uncertainty>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "common"; }
   static auto CLASS() { return "Uncertainty"; }
   static auto FIELD() { return "uncertainty"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         --Child<std::optional<common::Covariance>>("covariance") |
         --Child<std::optional<sfy::Standard>>("standard") |
         --Child<std::optional<mean::ListOfCovariances>>("listOfCovariances") |
         --Child<std::optional<reduced::Polynomial1d>>("polynomial1d")
      ;
   }

public:
   using Component::construct;

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::optional<common::Covariance>> covariance{this};
   Field<std::optional<sfy::Standard>> standard{this};
   Field<std::optional<mean::ListOfCovariances>> listOfCovariances{this};
   Field<std::optional<reduced::Polynomial1d>> polynomial1d{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->comment, \
      this->covariance, \
      this->standard, \
      this->listOfCovariances, \
      this->polynomial1d)

   // default
   Uncertainty() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Uncertainty(
      const wrapper<std::optional<common::Covariance>> &covariance,
      const wrapper<std::optional<sfy::Standard>> &standard = {},
      const wrapper<std::optional<mean::ListOfCovariances>> &listOfCovariances = {},
      const wrapper<std::optional<reduced::Polynomial1d>> &polynomial1d = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      covariance(this,covariance),
      standard(this,standard),
      listOfCovariances(this,listOfCovariances),
      polynomial1d(this,polynomial1d)
   {
      Component::finish();
   }

   // from node
   explicit Uncertainty(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Uncertainty(const Uncertainty &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      covariance(this,other.covariance),
      standard(this,other.standard),
      listOfCovariances(this,other.listOfCovariances),
      polynomial1d(this,other.polynomial1d)
   {
      Component::finish(other);
   }

   // move
   Uncertainty(Uncertainty &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      covariance(this,std::move(other.covariance)),
      standard(this,std::move(other.standard)),
      listOfCovariances(this,std::move(other.listOfCovariances)),
      polynomial1d(this,std::move(other.polynomial1d))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   Uncertainty &operator=(const Uncertainty &) = default;
   Uncertainty &operator=(Uncertainty &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/common/Uncertainty/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Uncertainty

} // namespace common
} // namespace v2_0
} // namespace alpha

#endif
