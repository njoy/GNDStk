
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_COMMON_DECAYPATH
#define ALPHA_V2_0_COMMON_DECAYPATH

#include "alpha/v2.0/common/Decay.hpp"

namespace alpha {
namespace v2_0 {
namespace common {

// -----------------------------------------------------------------------------
// common::
// class DecayPath
// -----------------------------------------------------------------------------

class DecayPath :
   public Component<common::DecayPath>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "common"; }
   static auto CLASS() { return "DecayPath"; }
   static auto FIELD() { return "decayPath"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<common::Decay>
            ("decay")
      ;
   }

public:
   using Component::construct;

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<common::Decay>>
      decay{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->comment, \
      this->decay)

   // default
   DecayPath() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit DecayPath(
      const wrapper<std::vector<common::Decay>>
         &decay
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      decay(this,decay)
   {
      Component::finish();
   }

   // from node
   explicit DecayPath(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   DecayPath(const DecayPath &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      decay(this,other.decay)
   {
      Component::finish(other);
   }

   // move
   DecayPath(DecayPath &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      decay(this,std::move(other.decay))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   DecayPath &operator=(const DecayPath &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         decay = other.decay;
      }
      return *this;
   }

   // move
   DecayPath &operator=(DecayPath &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         decay = std::move(other.decay);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/common/DecayPath/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class DecayPath

} // namespace common
} // namespace v2_0
} // namespace alpha

#endif
