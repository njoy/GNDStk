
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_GENERAL_PRODUCTIONS
#define ALPHA_V2_0_GENERAL_PRODUCTIONS

#include "alpha/v2.0/general/Production.hpp"

namespace alpha {
namespace v2_0 {
namespace general {

// -----------------------------------------------------------------------------
// general::
// class Productions
// -----------------------------------------------------------------------------

class Productions :
   public Component<general::Productions>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "general"; }
   static auto CLASS() { return "Productions"; }
   static auto FIELD() { return "productions"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<general::Production>
            ("production")
      ;
   }

public:
   using component_t = Component;
   using Component::construct;

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<general::Production>>
      production{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->production \
   )

   // default
   Productions() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Productions(
      const wrapper<std::vector<general::Production>>
         &production
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      production(this,production)
   {
      Component::finish();
   }

   // from node
   explicit Productions(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Productions(const Productions &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      production(this,other.production)
   {
      Component::finish(other);
   }

   // move
   Productions(Productions &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      production(this,std::move(other.production))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   Productions &operator=(const Productions &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         production = other.production;
      }
      return *this;
   }

   // move
   Productions &operator=(Productions &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         production = std::move(other.production);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/general/Productions/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Productions

} // namespace general
} // namespace v2_0
} // namespace alpha

#endif
