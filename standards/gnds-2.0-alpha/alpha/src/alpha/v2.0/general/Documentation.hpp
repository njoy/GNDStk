
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_GENERAL_DOCUMENTATION
#define ALPHA_V2_0_GENERAL_DOCUMENTATION

#include "alpha/v2.0/general/Authors.hpp"
#include "alpha/v2.0/general/Dates.hpp"
#include "alpha/v2.0/general/Title.hpp"
#include "alpha/v2.0/general/Body.hpp"
#include "alpha/v2.0/general/EndfCompatible.hpp"

namespace alpha {
namespace v2_0 {
namespace general {

// -----------------------------------------------------------------------------
// general::
// class Documentation
// -----------------------------------------------------------------------------

class Documentation :
   public Component<general::Documentation>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "general"; }
   static auto CLASS() { return "Documentation"; }
   static auto NODENAME() { return "documentation"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         --Child<general::Authors>
            ("authors") |
         --Child<general::Dates>
            ("dates") |
         --Child<general::Title>
            ("title") |
         --Child<general::Body>
            ("body") |
         --Child<std::optional<general::EndfCompatible>>
            ("endfCompatible")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "authors",
         "dates",
         "title",
         "body",
         "endfCompatible"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "authors",
         "dates",
         "title",
         "body",
         "endf_compatible"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<general::Authors>
      authors{this};
   Field<general::Dates>
      dates{this};
   Field<general::Title>
      title{this};
   Field<general::Body>
      body{this};
   Field<std::optional<general::EndfCompatible>>
      endfCompatible{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->authors, \
      this->dates, \
      this->title, \
      this->body, \
      this->endfCompatible \
   )

   // default
   Documentation() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Documentation(
      const wrapper<general::Authors>
         &authors,
      const wrapper<general::Dates>
         &dates = {},
      const wrapper<general::Title>
         &title = {},
      const wrapper<general::Body>
         &body = {},
      const wrapper<std::optional<general::EndfCompatible>>
         &endfCompatible = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      authors(this,authors),
      dates(this,dates),
      title(this,title),
      body(this,body),
      endfCompatible(this,endfCompatible)
   {
      Component::finish();
   }

   // from node
   explicit Documentation(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Documentation(const Documentation &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      authors(this,other.authors),
      dates(this,other.dates),
      title(this,other.title),
      body(this,other.body),
      endfCompatible(this,other.endfCompatible)
   {
      Component::finish(other);
   }

   // move
   Documentation(Documentation &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      authors(this,std::move(other.authors)),
      dates(this,std::move(other.dates)),
      title(this,std::move(other.title)),
      body(this,std::move(other.body)),
      endfCompatible(this,std::move(other.endfCompatible))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   Documentation &operator=(const Documentation &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         authors = other.authors;
         dates = other.dates;
         title = other.title;
         body = other.body;
         endfCompatible = other.endfCompatible;
      }
      return *this;
   }

   // move
   Documentation &operator=(Documentation &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         authors = std::move(other.authors);
         dates = std::move(other.dates);
         title = std::move(other.title);
         body = std::move(other.body);
         endfCompatible = std::move(other.endfCompatible);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/general/Documentation/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Documentation

} // namespace general
} // namespace v2_0
} // namespace alpha

#endif
