
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_GENERAL_DECAYMODE
#define ALPHA_V2_0_GENERAL_DECAYMODE

#include "alpha/v2.0/general/Probability.hpp"
#include "alpha/v2.0/general/DecayPath.hpp"
#include "alpha/v2.0/general/PhotonEmissionProbabilities.hpp"
#include "alpha/v2.0/general/Q.hpp"
#include "alpha/v2.0/general/Spectra.hpp"

namespace alpha {
namespace v2_0 {
namespace general {

// -----------------------------------------------------------------------------
// general::
// class DecayMode
// -----------------------------------------------------------------------------

class DecayMode :
   public Component<general::DecayMode>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, and a field/node of this type
   static auto NAMESPACE() { return "general"; }
   static auto CLASS() { return "DecayMode"; }
   static auto FIELD() { return "decayMode"; }

   // Core Interface multi-query to transfer information to/from Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // metadata
         std::optional<std::string>{}
            / Meta<>("label") |
         std::optional<std::string>{}
            / Meta<>("mode") |

         // children
         --Child<general::Probability>
            ("probability") |
         --Child<general::DecayPath>
            ("decayPath") |
         --Child<std::optional<general::PhotonEmissionProbabilities>>
            ("photonEmissionProbabilities") |
         --Child<std::optional<general::Q>>
            ("Q") |
         --Child<std::optional<general::Spectra>>
            ("spectra")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "label",
         "mode",
         "probability",
         "decayPath",
         "photonEmissionProbabilities",
         "Q",
         "spectra"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "label",
         "mode",
         "probability",
         "decay_path",
         "photon_emission_probabilities",
         "q",
         "spectra"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // metadata
   Field<std::optional<std::string>>
      label{this};
   Field<std::optional<std::string>>
      mode{this};

   // children
   Field<general::Probability>
      probability{this};
   Field<general::DecayPath>
      decayPath{this};
   Field<std::optional<general::PhotonEmissionProbabilities>>
      photonEmissionProbabilities{this};
   Field<std::optional<general::Q>>
      Q{this};
   Field<std::optional<general::Spectra>>
      spectra{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->label, \
      this->mode, \
      this->probability, \
      this->decayPath, \
      this->photonEmissionProbabilities, \
      this->Q, \
      this->spectra \
   )

   // default
   DecayMode() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit DecayMode(
      const wrapper<std::optional<std::string>>
         &label,
      const wrapper<std::optional<std::string>>
         &mode = {},
      const wrapper<general::Probability>
         &probability = {},
      const wrapper<general::DecayPath>
         &decayPath = {},
      const wrapper<std::optional<general::PhotonEmissionProbabilities>>
         &photonEmissionProbabilities = {},
      const wrapper<std::optional<general::Q>>
         &Q = {},
      const wrapper<std::optional<general::Spectra>>
         &spectra = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      label(this,label),
      mode(this,mode),
      probability(this,probability),
      decayPath(this,decayPath),
      photonEmissionProbabilities(this,photonEmissionProbabilities),
      Q(this,Q),
      spectra(this,spectra)
   {
      Component::finish();
   }

   // from node
   explicit DecayMode(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   DecayMode(const DecayMode &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      label(this,other.label),
      mode(this,other.mode),
      probability(this,other.probability),
      decayPath(this,other.decayPath),
      photonEmissionProbabilities(this,other.photonEmissionProbabilities),
      Q(this,other.Q),
      spectra(this,other.spectra)
   {
      Component::finish(other);
   }

   // move
   DecayMode(DecayMode &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      label(this,std::move(other.label)),
      mode(this,std::move(other.mode)),
      probability(this,std::move(other.probability)),
      decayPath(this,std::move(other.decayPath)),
      photonEmissionProbabilities(this,std::move(other.photonEmissionProbabilities)),
      Q(this,std::move(other.Q)),
      spectra(this,std::move(other.spectra))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   DecayMode &operator=(const DecayMode &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         label = other.label;
         mode = other.mode;
         probability = other.probability;
         decayPath = other.decayPath;
         photonEmissionProbabilities = other.photonEmissionProbabilities;
         Q = other.Q;
         spectra = other.spectra;
      }
      return *this;
   }

   // move
   DecayMode &operator=(DecayMode &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         label = std::move(other.label);
         mode = std::move(other.mode);
         probability = std::move(other.probability);
         decayPath = std::move(other.decayPath);
         photonEmissionProbabilities = std::move(other.photonEmissionProbabilities);
         Q = std::move(other.Q);
         spectra = std::move(other.spectra);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/general/DecayMode/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class DecayMode

} // namespace general
} // namespace v2_0
} // namespace alpha

#endif
