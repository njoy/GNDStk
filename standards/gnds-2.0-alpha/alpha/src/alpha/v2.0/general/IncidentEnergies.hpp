
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef ALPHA_V2_0_GENERAL_INCIDENTENERGIES
#define ALPHA_V2_0_GENERAL_INCIDENTENERGIES

#include "alpha/v2.0/general/IncidentEnergy.hpp"

namespace alpha {
namespace v2_0 {
namespace general {

// -----------------------------------------------------------------------------
// general::
// class IncidentEnergies
// -----------------------------------------------------------------------------

class IncidentEnergies :
   public Component<general::IncidentEnergies>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "general"; }
   static auto CLASS() { return "IncidentEnergies"; }
   static auto NODENAME() { return "incidentEnergies"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<general::IncidentEnergy>
            ("incidentEnergy")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "incidentEnergy"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "incident_energy"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<general::IncidentEnergy>>
      incidentEnergy{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->incidentEnergy \
   )

   // default
   IncidentEnergies() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit IncidentEnergies(
      const wrapper<std::vector<general::IncidentEnergy>>
         &incidentEnergy
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      incidentEnergy(this,incidentEnergy)
   {
      Component::finish();
   }

   // from node
   explicit IncidentEnergies(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   IncidentEnergies(const IncidentEnergies &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      incidentEnergy(this,other.incidentEnergy)
   {
      Component::finish(other);
   }

   // move
   IncidentEnergies(IncidentEnergies &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      incidentEnergy(this,std::move(other.incidentEnergy))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   IncidentEnergies &operator=(const IncidentEnergies &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         incidentEnergy = other.incidentEnergy;
      }
      return *this;
   }

   // move
   IncidentEnergies &operator=(IncidentEnergies &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         incidentEnergy = std::move(other.incidentEnergy);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "alpha/v2.0/general/IncidentEnergies/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class IncidentEnergies

} // namespace general
} // namespace v2_0
} // namespace alpha

#endif
