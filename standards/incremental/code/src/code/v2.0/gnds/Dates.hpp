
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_GNDS_DATES
#define TEST_V2_0_GNDS_DATES

#include "test/v2.0/gnds/Date.hpp"

namespace test {
namespace v2_0 {
namespace gnds {

// -----------------------------------------------------------------------------
// gnds::
// class Dates
// -----------------------------------------------------------------------------

class Dates : public Component<gnds::Dates> {
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, a field/node of this type
   static auto NAMESPACE() { return "gnds"; }
   static auto CLASS() { return "Dates"; }
   static auto FIELD() { return "dates"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto KEYS()
   {
      return
         // children
         gnds::Date{}
            / ++Child<>("date")
      ;
   }

public:
   using Component::construct;

   // children
   Field<std::vector<gnds::Date>> date{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->date)

   // default, and from fields
   explicit Dates(
      const wrapper<std::vector<gnds::Date>> &date = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      date(this,date)
   {
      std::cout << "ctor: Dates: 1. default/parameters" << std::endl;
      Component::finish();
   }

   // from node
   explicit Dates(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      std::cout << "ctor: Dates: 2. node" << std::endl;
      Component::finish(node);
   }

   // copy
   Dates(const Dates &other) :
      GNDSTK_COMPONENT(other.baseBlockData())
   {
      std::cout << "ctor: Dates: 4. copy" << std::endl;
      *this = other;
      Component::finish(other);
   }

   // move
   Dates(Dates &&other) :
      GNDSTK_COMPONENT(other.baseBlockData())
   {
      std::cout << "ctor: Dates: 5. move" << std::endl;
      *this = std::move(other);
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   Dates &operator=(const Dates &) = default;
   Dates &operator=(Dates &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/gnds/Dates/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Dates

} // namespace gnds
} // namespace v2_0
} // namespace test

#endif
