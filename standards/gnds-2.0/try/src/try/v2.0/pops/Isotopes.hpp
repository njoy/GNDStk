
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TRY_V2_0_POPS_ISOTOPES
#define TRY_V2_0_POPS_ISOTOPES

#include "try/v2.0/pops/Isotope.hpp"

namespace try {
namespace v2_0 {
namespace pops {

// -----------------------------------------------------------------------------
// pops::
// class Isotopes
// -----------------------------------------------------------------------------

class Isotopes : public Component<pops::Isotopes> {
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, a field/node of this type
   static auto NAMESPACE() { return "pops"; }
   static auto CLASS() { return "Isotopes"; }
   static auto FIELD() { return "isotopes"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto KEYS()
   {
      return
         // children
         pops::Isotope{}
            / ++Child<>("isotope")
      ;
   }

public:
   using Component::construct;

   // children
   Field<std::vector<pops::Isotope>> isotope{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->isotope)

   // default, and from fields
   explicit Isotopes(
      const wrapper<std::vector<pops::Isotope>> &isotope = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      isotope(this,isotope)
   {
      Component::finish();
   }

   // from node
   explicit Isotopes(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Isotopes(const Isotopes &other) :
      GNDSTK_COMPONENT(other.baseBlockData())
   {
      *this = other;
      Component::finish(other);
   }

   // move
   Isotopes(Isotopes &&other) :
      GNDSTK_COMPONENT(other.baseBlockData())
   {
      *this = std::move(other);
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   Isotopes &operator=(const Isotopes &) = default;
   Isotopes &operator=(Isotopes &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "try/v2.0/pops/Isotopes/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Isotopes

} // namespace pops
} // namespace v2_0
} // namespace try

#endif
