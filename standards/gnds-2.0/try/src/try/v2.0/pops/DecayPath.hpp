
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TRY_V2_0_POPS_DECAYPATH
#define TRY_V2_0_POPS_DECAYPATH

#include "try/v2.0/pops/Decay.hpp"

namespace try {
namespace v2_0 {
namespace pops {

// -----------------------------------------------------------------------------
// pops::
// class DecayPath
// -----------------------------------------------------------------------------

class DecayPath : public Component<pops::DecayPath> {
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, a field/node of this type
   static auto NAMESPACE() { return "pops"; }
   static auto CLASS() { return "DecayPath"; }
   static auto FIELD() { return "decayPath"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto KEYS()
   {
      return
         // children
         pops::Decay{}
            / ++Child<>("decay")
      ;
   }

public:
   using Component::construct;

   // children
   Field<std::vector<pops::Decay>> decay{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->decay)

   // default, and from fields
   explicit DecayPath(
      const wrapper<std::vector<pops::Decay>> &decay = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      decay(this,decay)
   {
      Component::finish();
   }

   // from node
   explicit DecayPath(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   DecayPath(const DecayPath &other) :
      GNDSTK_COMPONENT(other.baseBlockData())
   {
      *this = other;
      Component::finish(other);
   }

   // move
   DecayPath(DecayPath &&other) :
      GNDSTK_COMPONENT(other.baseBlockData())
   {
      *this = std::move(other);
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   DecayPath &operator=(const DecayPath &) = default;
   DecayPath &operator=(DecayPath &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "try/v2.0/pops/DecayPath/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class DecayPath

} // namespace pops
} // namespace v2_0
} // namespace try

#endif
