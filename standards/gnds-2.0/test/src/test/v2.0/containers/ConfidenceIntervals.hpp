
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_CONTAINERS_CONFIDENCEINTERVALS
#define TEST_V2_0_CONTAINERS_CONFIDENCEINTERVALS

#include "test/v2.0/containers/Interval.hpp"

namespace test {
namespace v2_0 {
namespace containers {

// -----------------------------------------------------------------------------
// containers::
// class ConfidenceIntervals
// -----------------------------------------------------------------------------

class ConfidenceIntervals :
   public Component<containers::ConfidenceIntervals>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "containers"; }
   static auto CLASS() { return "ConfidenceIntervals"; }
   static auto NODENAME() { return "confidenceIntervals"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<containers::Interval>
            ("interval")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "interval"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "interval"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<containers::Interval>>
      interval{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->interval \
   )

   // default
   ConfidenceIntervals() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit ConfidenceIntervals(
      const wrapper<std::vector<containers::Interval>>
         &interval
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      interval(this,interval)
   {
      Component::finish();
   }

   // from node
   explicit ConfidenceIntervals(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   ConfidenceIntervals(const ConfidenceIntervals &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      interval(this,other.interval)
   {
      Component::finish(other);
   }

   // move
   ConfidenceIntervals(ConfidenceIntervals &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      interval(this,std::move(other.interval))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   ConfidenceIntervals &operator=(const ConfidenceIntervals &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         interval = other.interval;
      }
      return *this;
   }

   // move
   ConfidenceIntervals &operator=(ConfidenceIntervals &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         interval = std::move(other.interval);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/containers/ConfidenceIntervals/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class ConfidenceIntervals

} // namespace containers
} // namespace v2_0
} // namespace test

#endif
