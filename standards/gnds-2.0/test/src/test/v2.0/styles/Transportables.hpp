
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_STYLES_TRANSPORTABLES
#define TEST_V2_0_STYLES_TRANSPORTABLES

#include "test/v2.0/styles/Transportable.hpp"

namespace test {
namespace v2_0 {
namespace styles {

// -----------------------------------------------------------------------------
// styles::
// class Transportables
// -----------------------------------------------------------------------------

class Transportables :
   public Component<styles::Transportables>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "styles"; }
   static auto CLASS() { return "Transportables"; }
   static auto NODENAME() { return "transportables"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // metadata
         std::optional<XMLName>{}
            / Meta<>("label") |

         // children
         ++Child<styles::Transportable>
            ("transportable")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "label",
         "transportable"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "label",
         "transportable"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // metadata
   Field<std::optional<XMLName>>
      label{this};

   // children
   Field<std::vector<styles::Transportable>>
      transportable{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->label, \
      this->transportable \
   )

   // default
   Transportables() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Transportables(
      const wrapper<std::optional<XMLName>>
         &label,
      const wrapper<std::vector<styles::Transportable>>
         &transportable = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      label(this,label),
      transportable(this,transportable)
   {
      Component::finish();
   }

   // from node
   explicit Transportables(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Transportables(const Transportables &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      label(this,other.label),
      transportable(this,other.transportable)
   {
      Component::finish(other);
   }

   // move
   Transportables(Transportables &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      label(this,std::move(other.label)),
      transportable(this,std::move(other.transportable))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   Transportables &operator=(const Transportables &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         label = other.label;
         transportable = other.transportable;
      }
      return *this;
   }

   // move
   Transportables &operator=(Transportables &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         label = std::move(other.label);
         transportable = std::move(other.transportable);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/styles/Transportables/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Transportables

} // namespace styles
} // namespace v2_0
} // namespace test

#endif
