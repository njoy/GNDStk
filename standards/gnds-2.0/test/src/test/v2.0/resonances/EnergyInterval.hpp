
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_RESONANCES_ENERGYINTERVAL
#define TEST_V2_0_RESONANCES_ENERGYINTERVAL

#include "test/v2.0/resonances/RMatrix.hpp"
#include "test/v2.0/resonances/BreitWigner.hpp"

namespace test {
namespace v2_0 {
namespace resonances {

// -----------------------------------------------------------------------------
// resonances::
// class EnergyInterval
// -----------------------------------------------------------------------------

class EnergyInterval : public Component<resonances::EnergyInterval> {
   friend class Component;

   using _t = std::variant<
      resonances::RMatrix,
      resonances::BreitWigner
   >;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, a field/node of this type
   static auto NAMESPACE() { return "resonances"; }
   static auto CLASS() { return "EnergyInterval"; }
   static auto FIELD() { return "energyInterval"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto KEYS()
   {
      return
         // metadata
         Integer32{}
            / Meta<>("index") |
         Float64{}
            / Meta<>("domainMin") |
         Float64{}
            / Meta<>("domainMax") |
         XMLName{}
            / Meta<>("domainUnit") |
         // children
         _t{}
            / --(Child<>("RMatrix") || Child<>("BreitWigner"))
      ;
   }

public:
   using Component::construct;

   // metadata
   Field<Integer32> index{this};
   Field<Float64> domainMin{this};
   Field<Float64> domainMax{this};
   Field<XMLName> domainUnit{this};

   // children - variant
   Field<_t> _RMatrixBreitWigner{this};
   FieldPart<decltype(_RMatrixBreitWigner),resonances::RMatrix> RMatrix{_RMatrixBreitWigner};
   FieldPart<decltype(_RMatrixBreitWigner),resonances::BreitWigner> BreitWigner{_RMatrixBreitWigner};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->index, \
      this->domainMin, \
      this->domainMax, \
      this->domainUnit, \
      this->_RMatrixBreitWigner)

   // default
   EnergyInterval() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields
   explicit EnergyInterval(
      const wrapper<Integer32> &index,
      const wrapper<Float64> &domainMin = {},
      const wrapper<Float64> &domainMax = {},
      const wrapper<XMLName> &domainUnit = {},
      const wrapper<_t> &_RMatrixBreitWigner = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      index(this,index),
      domainMin(this,domainMin),
      domainMax(this,domainMax),
      domainUnit(this,domainUnit),
      _RMatrixBreitWigner(this,_RMatrixBreitWigner)
   {
      Component::finish();
   }

   // from node
   explicit EnergyInterval(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   EnergyInterval(const EnergyInterval &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      index(this,other.index),
      domainMin(this,other.domainMin),
      domainMax(this,other.domainMax),
      domainUnit(this,other.domainUnit),
      _RMatrixBreitWigner(this,other._RMatrixBreitWigner)
   {
      Component::finish(other);
   }

   // move
   EnergyInterval(EnergyInterval &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      index(this,std::move(other.index)),
      domainMin(this,std::move(other.domainMin)),
      domainMax(this,std::move(other.domainMax)),
      domainUnit(this,std::move(other.domainUnit)),
      _RMatrixBreitWigner(this,std::move(other._RMatrixBreitWigner))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   EnergyInterval &operator=(const EnergyInterval &) = default;
   EnergyInterval &operator=(EnergyInterval &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/resonances/EnergyInterval/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class EnergyInterval

} // namespace resonances
} // namespace v2_0
} // namespace test

#endif
