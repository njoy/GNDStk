
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_TSL_DISTINCTSCATTERINGKERNEL
#define TEST_V2_0_TSL_DISTINCTSCATTERINGKERNEL

#include "test/v2.0/containers/Gridded3d.hpp"

namespace test {
namespace v2_0 {
namespace tsl {

// -----------------------------------------------------------------------------
// tsl::
// class DistinctScatteringKernel
// -----------------------------------------------------------------------------

class DistinctScatteringKernel :
   public Component<tsl::DistinctScatteringKernel>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "tsl"; }
   static auto CLASS() { return "DistinctScatteringKernel"; }
   static auto NODENAME() { return "distinctScatteringKernel"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         --Child<std::optional<containers::Gridded3d>>
            ("gridded3d")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "gridded3d"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "gridded3d"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::optional<containers::Gridded3d>>
      gridded3d{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->gridded3d \
   )

   // default
   DistinctScatteringKernel() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit DistinctScatteringKernel(
      const wrapper<std::optional<containers::Gridded3d>>
         &gridded3d
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      gridded3d(this,gridded3d)
   {
      Component::finish();
   }

   // from node
   explicit DistinctScatteringKernel(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   DistinctScatteringKernel(const DistinctScatteringKernel &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      gridded3d(this,other.gridded3d)
   {
      Component::finish(other);
   }

   // move
   DistinctScatteringKernel(DistinctScatteringKernel &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      gridded3d(this,std::move(other.gridded3d))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   DistinctScatteringKernel &operator=(const DistinctScatteringKernel &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         gridded3d = other.gridded3d;
      }
      return *this;
   }

   // move
   DistinctScatteringKernel &operator=(DistinctScatteringKernel &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         gridded3d = std::move(other.gridded3d);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/tsl/DistinctScatteringKernel/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class DistinctScatteringKernel

} // namespace tsl
} // namespace v2_0
} // namespace test

#endif
