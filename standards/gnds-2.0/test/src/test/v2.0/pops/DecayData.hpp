
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_POPS_DECAYDATA
#define TEST_V2_0_POPS_DECAYDATA

#include "test/v2.0/pops/DecayModes.hpp"
#include "test/v2.0/pops/AverageEnergies.hpp"

namespace test {
namespace v2_0 {
namespace pops {

// -----------------------------------------------------------------------------
// pops::
// class DecayData
// -----------------------------------------------------------------------------

class DecayData :
   public Component<pops::DecayData>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "pops"; }
   static auto CLASS() { return "DecayData"; }
   static auto NODENAME() { return "decayData"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         --Child<std::optional<pops::DecayModes>>
            ("decayModes") |
         --Child<std::optional<pops::AverageEnergies>>
            ("averageEnergies")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "decayModes",
         "averageEnergies"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "decay_modes",
         "average_energies"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::optional<pops::DecayModes>>
      decayModes{this};
   Field<std::optional<pops::AverageEnergies>>
      averageEnergies{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->decayModes, \
      this->averageEnergies \
   )

   // default
   DecayData() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit DecayData(
      const wrapper<std::optional<pops::DecayModes>>
         &decayModes,
      const wrapper<std::optional<pops::AverageEnergies>>
         &averageEnergies = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      decayModes(this,decayModes),
      averageEnergies(this,averageEnergies)
   {
      Component::finish();
   }

   // from node
   explicit DecayData(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   DecayData(const DecayData &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      decayModes(this,other.decayModes),
      averageEnergies(this,other.averageEnergies)
   {
      Component::finish(other);
   }

   // move
   DecayData(DecayData &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      decayModes(this,std::move(other.decayModes)),
      averageEnergies(this,std::move(other.averageEnergies))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   DecayData &operator=(const DecayData &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         decayModes = other.decayModes;
         averageEnergies = other.averageEnergies;
      }
      return *this;
   }

   // move
   DecayData &operator=(DecayData &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         decayModes = std::move(other.decayModes);
         averageEnergies = std::move(other.averageEnergies);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/pops/DecayData/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class DecayData

} // namespace pops
} // namespace v2_0
} // namespace test

#endif
