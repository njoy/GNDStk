
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_POPS_POSITRONEMISSIONINTENSITY
#define TEST_V2_0_POPS_POSITRONEMISSIONINTENSITY

#include "test/v2.0/pops/Uncertainty.hpp"

namespace test {
namespace v2_0 {
namespace pops {

// -----------------------------------------------------------------------------
// pops::
// class PositronEmissionIntensity
// -----------------------------------------------------------------------------

class PositronEmissionIntensity : public Component<pops::PositronEmissionIntensity> {
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace, this class, a field/node of this type
   static auto NAMESPACE() { return "pops"; }
   static auto CLASS() { return "PositronEmissionIntensity"; }
   static auto FIELD() { return "positronEmissionIntensity"; }

   // Core Interface multi-query to extract metadata and child nodes
   static auto KEYS()
   {
      return
         // metadata
         Float64{}
            / Meta<>("value") |
         // children
         --Child<std::optional<pops::Uncertainty>>("uncertainty")
      ;
   }

public:
   using Component::construct;

   // metadata
   Field<Float64> value{this};

   // children
   Field<std::optional<pops::Uncertainty>> uncertainty{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) Component(blockdata, \
      this->value, \
      this->uncertainty)

   // default
   PositronEmissionIntensity() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields
   explicit PositronEmissionIntensity(
      const wrapper<Float64> &value,
      const wrapper<std::optional<pops::Uncertainty>> &uncertainty = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      value(this,value),
      uncertainty(this,uncertainty)
   {
      Component::finish();
   }

   // from node
   explicit PositronEmissionIntensity(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   PositronEmissionIntensity(const PositronEmissionIntensity &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      value(this,other.value),
      uncertainty(this,other.uncertainty)
   {
      Component::finish(other);
   }

   // move
   PositronEmissionIntensity(PositronEmissionIntensity &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      value(this,std::move(other.value)),
      uncertainty(this,std::move(other.uncertainty))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   PositronEmissionIntensity &operator=(const PositronEmissionIntensity &) = default;
   PositronEmissionIntensity &operator=(PositronEmissionIntensity &&) = default;

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/pops/PositronEmissionIntensity/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class PositronEmissionIntensity

} // namespace pops
} // namespace v2_0
} // namespace test

#endif
