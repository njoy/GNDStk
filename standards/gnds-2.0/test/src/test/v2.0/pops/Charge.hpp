
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_POPS_CHARGE
#define TEST_V2_0_POPS_CHARGE

#include "test/v2.0/documentation/Documentation.hpp"
#include "test/v2.0/pops/Uncertainty.hpp"
#include "test/v2.0/containers/Integer.hpp"
#include "test/v2.0/containers/Fraction.hpp"

namespace test {
namespace v2_0 {
namespace pops {

// -----------------------------------------------------------------------------
// pops::
// class Charge
// -----------------------------------------------------------------------------

class Charge :
   public Component<pops::Charge>
{
   friend class Component;

   using _t = std::variant<
      containers::Integer,
      containers::Fraction
   >;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "pops"; }
   static auto CLASS() { return "Charge"; }
   static auto NODENAME() { return "charge"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // metadata
         std::optional<XMLName>{}
            / Meta<>("label") |
         std::optional<XMLName>{}
            / Meta<>("unit") |
         std::optional<XMLName>{}
            / Meta<>("value") |

         // children
         --Child<std::optional<documentation::Documentation>>
            ("documentation") |
         --Child<std::optional<pops::Uncertainty>>
            ("uncertainty") |
         _t{}
            / --(Child<>("integer") || Child<>("fraction"))
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "label",
         "unit",
         "value",
         "documentation",
         "uncertainty",
         "_integerfraction"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "label",
         "unit",
         "value",
         "documentation",
         "uncertainty",
         "_integerfraction"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // metadata
   Field<std::optional<XMLName>>
      label{this};
   Field<std::optional<XMLName>>
      unit{this};
   Field<std::optional<XMLName>>
      value{this};

   // children
   Field<std::optional<documentation::Documentation>>
      documentation{this};
   Field<std::optional<pops::Uncertainty>>
      uncertainty{this};

   // children - variant
   Field<_t>
      _integerfraction{this};
   FieldPart<decltype(_integerfraction),containers::Integer> integer{_integerfraction};
   FieldPart<decltype(_integerfraction),containers::Fraction> fraction{_integerfraction};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->label, \
      this->unit, \
      this->value, \
      this->documentation, \
      this->uncertainty, \
      this->_integerfraction \
   )

   // default
   Charge() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Charge(
      const wrapper<std::optional<XMLName>>
         &label,
      const wrapper<std::optional<XMLName>>
         &unit = {},
      const wrapper<std::optional<XMLName>>
         &value = {},
      const wrapper<std::optional<documentation::Documentation>>
         &documentation = {},
      const wrapper<std::optional<pops::Uncertainty>>
         &uncertainty = {},
      const wrapper<_t>
         &_integerfraction = {}
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      label(this,label),
      unit(this,unit),
      value(this,value),
      documentation(this,documentation),
      uncertainty(this,uncertainty),
      _integerfraction(this,_integerfraction)
   {
      Component::finish();
   }

   // from node
   explicit Charge(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Charge(const Charge &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      label(this,other.label),
      unit(this,other.unit),
      value(this,other.value),
      documentation(this,other.documentation),
      uncertainty(this,other.uncertainty),
      _integerfraction(this,other._integerfraction)
   {
      Component::finish(other);
   }

   // move
   Charge(Charge &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      label(this,std::move(other.label)),
      unit(this,std::move(other.unit)),
      value(this,std::move(other.value)),
      documentation(this,std::move(other.documentation)),
      uncertainty(this,std::move(other.uncertainty)),
      _integerfraction(this,std::move(other._integerfraction))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   Charge &operator=(const Charge &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         label = other.label;
         unit = other.unit;
         value = other.value;
         documentation = other.documentation;
         uncertainty = other.uncertainty;
         _integerfraction = other._integerfraction;
      }
      return *this;
   }

   // move
   Charge &operator=(Charge &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         label = std::move(other.label);
         unit = std::move(other.unit);
         value = std::move(other.value);
         documentation = std::move(other.documentation);
         uncertainty = std::move(other.uncertainty);
         _integerfraction = std::move(other._integerfraction);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/pops/Charge/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Charge

} // namespace pops
} // namespace v2_0
} // namespace test

#endif
