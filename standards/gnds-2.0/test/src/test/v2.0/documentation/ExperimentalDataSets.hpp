
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_DOCUMENTATION_EXPERIMENTALDATASETS
#define TEST_V2_0_DOCUMENTATION_EXPERIMENTALDATASETS

#include "test/v2.0/documentation/ExforDataSets.hpp"

namespace test {
namespace v2_0 {
namespace documentation {

// -----------------------------------------------------------------------------
// documentation::
// class ExperimentalDataSets
// -----------------------------------------------------------------------------

class ExperimentalDataSets :
   public Component<documentation::ExperimentalDataSets>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "documentation"; }
   static auto CLASS() { return "ExperimentalDataSets"; }
   static auto NODENAME() { return "experimentalDataSets"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         --Child<documentation::ExforDataSets>
            ("exforDataSets")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "exforDataSets"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "exfor_data_sets"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<documentation::ExforDataSets>
      exforDataSets{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->exforDataSets \
   )

   // default
   ExperimentalDataSets() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit ExperimentalDataSets(
      const wrapper<documentation::ExforDataSets>
         &exforDataSets
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      exforDataSets(this,exforDataSets)
   {
      Component::finish();
   }

   // from node
   explicit ExperimentalDataSets(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   ExperimentalDataSets(const ExperimentalDataSets &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      exforDataSets(this,other.exforDataSets)
   {
      Component::finish(other);
   }

   // move
   ExperimentalDataSets(ExperimentalDataSets &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      exforDataSets(this,std::move(other.exforDataSets))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   ExperimentalDataSets &operator=(const ExperimentalDataSets &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         exforDataSets = other.exforDataSets;
      }
      return *this;
   }

   // move
   ExperimentalDataSets &operator=(ExperimentalDataSets &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         exforDataSets = std::move(other.exforDataSets);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/documentation/ExperimentalDataSets/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class ExperimentalDataSets

} // namespace documentation
} // namespace v2_0
} // namespace test

#endif
