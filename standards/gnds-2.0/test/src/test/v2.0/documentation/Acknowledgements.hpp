
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_DOCUMENTATION_ACKNOWLEDGEMENTS
#define TEST_V2_0_DOCUMENTATION_ACKNOWLEDGEMENTS

#include "test/v2.0/documentation/Acknowledgement.hpp"

namespace test {
namespace v2_0 {
namespace documentation {

// -----------------------------------------------------------------------------
// documentation::
// class Acknowledgements
// -----------------------------------------------------------------------------

class Acknowledgements :
   public Component<documentation::Acknowledgements>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "documentation"; }
   static auto CLASS() { return "Acknowledgements"; }
   static auto NODENAME() { return "acknowledgements"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<documentation::Acknowledgement>
            ("acknowledgement")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "acknowledgement"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "acknowledgement"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<documentation::Acknowledgement>>
      acknowledgement{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->acknowledgement \
   )

   // default
   Acknowledgements() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Acknowledgements(
      const wrapper<std::vector<documentation::Acknowledgement>>
         &acknowledgement
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      acknowledgement(this,acknowledgement)
   {
      Component::finish();
   }

   // from node
   explicit Acknowledgements(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Acknowledgements(const Acknowledgements &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      acknowledgement(this,other.acknowledgement)
   {
      Component::finish(other);
   }

   // move
   Acknowledgements(Acknowledgements &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      acknowledgement(this,std::move(other.acknowledgement))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   Acknowledgements &operator=(const Acknowledgements &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         acknowledgement = other.acknowledgement;
      }
      return *this;
   }

   // move
   Acknowledgements &operator=(Acknowledgements &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         acknowledgement = std::move(other.acknowledgement);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/documentation/Acknowledgements/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Acknowledgements

} // namespace documentation
} // namespace v2_0
} // namespace test

#endif
