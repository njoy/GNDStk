
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_DOCUMENTATION_OUTPUTDECKS
#define TEST_V2_0_DOCUMENTATION_OUTPUTDECKS

#include "test/v2.0/documentation/OutputDeck.hpp"

namespace test {
namespace v2_0 {
namespace documentation {

// -----------------------------------------------------------------------------
// documentation::
// class OutputDecks
// -----------------------------------------------------------------------------

class OutputDecks :
   public Component<documentation::OutputDecks>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "documentation"; }
   static auto CLASS() { return "OutputDecks"; }
   static auto NODENAME() { return "outputDecks"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<documentation::OutputDeck>
            ("outputDeck")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "outputDeck"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "output_deck"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::vector<documentation::OutputDeck>>
      outputDeck{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->outputDeck \
   )

   // default
   OutputDecks() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit OutputDecks(
      const wrapper<std::vector<documentation::OutputDeck>>
         &outputDeck
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      outputDeck(this,outputDeck)
   {
      Component::finish();
   }

   // from node
   explicit OutputDecks(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   OutputDecks(const OutputDecks &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      outputDeck(this,other.outputDeck)
   {
      Component::finish(other);
   }

   // move
   OutputDecks(OutputDecks &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      outputDeck(this,std::move(other.outputDeck))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   OutputDecks &operator=(const OutputDecks &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         outputDeck = other.outputDeck;
      }
      return *this;
   }

   // move
   OutputDecks &operator=(OutputDecks &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         outputDeck = std::move(other.outputDeck);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/documentation/OutputDecks/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class OutputDecks

} // namespace documentation
} // namespace v2_0
} // namespace test

#endif
