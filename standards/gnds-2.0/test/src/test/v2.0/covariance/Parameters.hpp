
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef TEST_V2_0_COVARIANCE_PARAMETERS
#define TEST_V2_0_COVARIANCE_PARAMETERS

#include "test/v2.0/covariance/ParameterLink.hpp"

namespace test {
namespace v2_0 {
namespace covariance {

// -----------------------------------------------------------------------------
// covariance::
// class Parameters
// -----------------------------------------------------------------------------

class Parameters :
   public Component<covariance::Parameters>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "covariance"; }
   static auto CLASS() { return "Parameters"; }
   static auto NODENAME() { return "parameters"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // children
         ++Child<std::optional<covariance::ParameterLink>>
            ("parameterLink")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "parameterLink"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "parameter_link"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // children
   Field<std::optional<std::vector<covariance::ParameterLink>>>
      parameterLink{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->parameterLink \
   )

   // default
   Parameters() :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Parameters(
      const wrapper<std::optional<std::vector<covariance::ParameterLink>>>
         &parameterLink
   ) :
      GNDSTK_COMPONENT(BlockData{}),
      parameterLink(this,parameterLink)
   {
      Component::finish();
   }

   // from node
   explicit Parameters(const Node &node) :
      GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Parameters(const Parameters &other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      parameterLink(this,other.parameterLink)
   {
      Component::finish(other);
   }

   // move
   Parameters(Parameters &&other) :
      GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      parameterLink(this,std::move(other.parameterLink))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   Parameters &operator=(const Parameters &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         parameterLink = other.parameterLink;
      }
      return *this;
   }

   // move
   Parameters &operator=(Parameters &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         parameterLink = std::move(other.parameterLink);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "test/v2.0/covariance/Parameters/src/custom.hpp"
   #undef GNDSTK_COMPONENT
}; // class Parameters

} // namespace covariance
} // namespace v2_0
} // namespace test

#endif
