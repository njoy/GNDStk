
// THIS FILE WAS AUTOGENERATED!
// DO NOT MODIFY!

#ifndef GNDS_V2_0_GENERAL_RESONANCES
#define GNDS_V2_0_GENERAL_RESONANCES

#include "gnds/v2.0/general/ScatteringRadius.hpp"
#include "gnds/v2.0/general/Resolved.hpp"
#include "gnds/v2.0/general/Unresolved.hpp"

namespace gnds {
namespace v2_0 {
namespace general {

// -----------------------------------------------------------------------------
// general::
// class Resonances
// -----------------------------------------------------------------------------

class Resonances :
   public Component<general::Resonances>
{
   friend class Component;

   // ------------------------
   // For Component
   // ------------------------

   // Names: this namespace and class, and original nodes (as in XML <...>)
   static auto NAMESPACE() { return "general"; }
   static auto CLASS() { return "Resonances"; }
   static auto NODENAME() { return "resonances"; }

   // Core Interface multi-query to transfer information to/from core Nodes
   static auto KEYS()
   {
      return
         // comment
         ++Child<std::string>(special::comment) / CommentConverter{} |

         // metadata
         std::optional<std::string>{}
            / Meta<>("href") |

         // children
         --Child<std::optional<general::ScatteringRadius>>
            ("scatteringRadius") |
         --Child<std::optional<general::Resolved>>
            ("resolved") |
         --Child<std::optional<general::Unresolved>>
            ("unresolved")
      ;
   }

   // Data member names. Usually - but not necessarily - the same as the node
   // names appearing in KEYS(). These are used by Component's prettyprinter.
   static const auto &FIELDNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "href",
         "scatteringRadius",
         "resolved",
         "unresolved"
      };
      return names;
   }

   // Data member names, as they'll be presented in the Python bindings.
   static const auto &PYTHONNAMES()
   {
      static const std::vector<std::string> names = {
         "comment",
         "href",
         "scattering_radius",
         "resolved",
         "unresolved"
      };
      return names;
   }

   // ------------------------
   // Public interface
   // ------------------------

public:

   using component_t = Component;
   using Component::construct;

   // ------------------------
   // Data members
   // ------------------------

   // comment
   Field<std::vector<std::string>> comment{this};

   // metadata
   Field<std::optional<std::string>>
      href{this};

   // children
   Field<std::optional<general::ScatteringRadius>>
      scatteringRadius{this};
   Field<std::optional<general::Resolved>>
      resolved{this};
   Field<std::optional<general::Unresolved>>
      unresolved{this};

   // ------------------------
   // Constructors
   // ------------------------

   #define NJOY_GNDSTK_COMPONENT(blockdata) \
   Component( \
      blockdata, \
      this->comment, \
      this->href, \
      this->scatteringRadius, \
      this->resolved, \
      this->unresolved \
   )

   // default
   Resonances() :
      NJOY_GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish();
   }

   // from fields, comment excluded
   explicit Resonances(
      const wrapper<std::optional<std::string>>
         &href,
      const wrapper<std::optional<general::ScatteringRadius>>
         &scatteringRadius = {},
      const wrapper<std::optional<general::Resolved>>
         &resolved = {},
      const wrapper<std::optional<general::Unresolved>>
         &unresolved = {}
   ) :
      NJOY_GNDSTK_COMPONENT(BlockData{}),
      href(this,href),
      scatteringRadius(this,scatteringRadius),
      resolved(this,resolved),
      unresolved(this,unresolved)
   {
      Component::finish();
   }

   // from node
   explicit Resonances(const Node &node) :
      NJOY_GNDSTK_COMPONENT(BlockData{})
   {
      Component::finish(node);
   }

   // copy
   Resonances(const Resonances &other) :
      NJOY_GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,other.comment),
      href(this,other.href),
      scatteringRadius(this,other.scatteringRadius),
      resolved(this,other.resolved),
      unresolved(this,other.unresolved)
   {
      Component::finish(other);
   }

   // move
   Resonances(Resonances &&other) :
      NJOY_GNDSTK_COMPONENT(other.baseBlockData()),
      comment(this,std::move(other.comment)),
      href(this,std::move(other.href)),
      scatteringRadius(this,std::move(other.scatteringRadius)),
      resolved(this,std::move(other.resolved)),
      unresolved(this,std::move(other.unresolved))
   {
      Component::finish(other);
   }

   // ------------------------
   // Assignment operators
   // ------------------------

   // copy
   Resonances &operator=(const Resonances &other)
   {
      if (this != &other) {
         Component::operator=(other);
         comment = other.comment;
         href = other.href;
         scatteringRadius = other.scatteringRadius;
         resolved = other.resolved;
         unresolved = other.unresolved;
      }
      return *this;
   }

   // move
   Resonances &operator=(Resonances &&other)
   {
      if (this != &other) {
         Component::operator=(std::move(other));
         comment = std::move(other.comment);
         href = std::move(other.href);
         scatteringRadius = std::move(other.scatteringRadius);
         resolved = std::move(other.resolved);
         unresolved = std::move(other.unresolved);
      }
      return *this;
   }

   // ------------------------
   // Custom functionality
   // ------------------------

   #include "gnds/v2.0/general/Resonances/src/custom.hpp"
   #undef NJOY_GNDSTK_COMPONENT
}; // class Resonances

} // namespace general
} // namespace v2_0
} // namespace gnds

#endif
